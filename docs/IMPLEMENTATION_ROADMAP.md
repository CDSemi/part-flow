# PartFlow Implementation Roadmap

> **Authority:** Canonical for implementation order, phase boundaries, dependencies, and temporary limitations.
> Domain behavior and product scope are defined by [`PROJECT_PROFILE.md`](./PROJECT_PROFILE.md); the approved target UI by [`GUI_DESIGN.md`](./GUI_DESIGN.md).
> **Language:** English is the source of truth. [Tiếng Việt](./IMPLEMENTATION_ROADMAP.vi.md).

## Current State

> **Coverage audit (2026-09-03):** a cross-check of `PROJECT_PROFILE.md`, `GUI_DESIGN.md`, this roadmap, and the real production code found one previously deferred production workflow with no owning later phase (`Receive Quantity` at the Scan Station), plus incomplete handoffs in monitoring, Worker sessions, authorized corrections, and Administration policy settings. Phase 10.5 is inserted as a named corrective prerequisite without renumbering later phases. Every remaining target behavior must be assigned below to an explicit owner phase or to `Deferred` / a canonical open decision.

- Canonical project specification: `PROJECT_PROFILE.md` (the v21 restricted-edit round — a Work Order Demand line with released quantity stays editable within limits instead of freezing: requested quantity never below the released or allocated quantity, plus due date and Job Numbers, with Part Number, Request Type, requester, reason and notes fixed and removal still refused, so raising the quantity of a fully released line returns remaining quantity to release and its Work Order to Open; building on the v20 release-granularity round — a Work Order Demand may be released in parts until its remaining quantity is exhausted, each part its own explicit release and its own Quantity Flow with nothing merged, the remaining quantity a hard limit, a released demand line never removable, and a terminal Area never a starting Area; building on the v19 badge-confirmation round — per-action final-confirmation gates for DONE / QUEUE return / Undo: a Worker badge scan as the last step after the confirmation summary in scanned-session Areas behind three Administration → Worker sessions options defaulting to on, a final toned confirmation question in fixed-Worker Areas, superseding the v18 "Undo needs no extra badge scan" rule; building on the v18 decision round closing the remaining GUI open questions — Workers as Scan-Station-scoped audit identity separate from Users, with profiles of stable id + name + badge barcode + avatar + active status and no employee number; Worker badge barcodes are the company's existing employee badges, exact-matched as non-`PF:` values with the former `PF:WORKER:` format removed; scanned Worker Sessions expiring through a configurable sliding inactivity timeout — Administration default with per-Area overrides, refresh on valid production interactions only, immediate Worker switch, and a Scan-Station-only blocking badge modal that preserves open dialog drafts; Undo Worker identity following the Area mode with the reversal recording the Worker active at confirmation; theme persistence per User and Scan Station with precedence User → Station → Dark default; deterministic Priority hot-add barcode resolution — one eligible demand adds directly, several require explicit selection; unlimited in-session Priority Undo/Redo depth; the Production Board confirmed Department-wide with per-Department rotation display settings; plus the v17 PN-identity model — the canonical PN string itself, uppercase and whitespace-free, is the stable domain identity; the PartNumber master is optional, hard-deletable current metadata; production records — WorkOrderDemand, QuantityFlow, PartMovement, Allocation — keep their own canonical PN value with no `part_number_id` surrogate linkage; plus the configurable Movement-history retention policy: export losslessly, verify, then purge — never the reverse; plus the earlier rules: Machine return-to-service — RETIRED → ACTIVE reactivation of the **same physical machine** on the same record with append-only `RETIRED`/`REACTIVATED` lifecycle audit events and a forward-only Area change when the machine moved while retired, blocked on reissued identity or active-name collisions; display names unique among the active Machines of one Area, with reuse across time/replacement still allowed; plus the v11 rules: Machine lifecycle with derived operational state — maintenance override wins, otherwise assigned quantity → Running, otherwise Idle, with a `state_changed_at` timestamp and derived elapsed-time display; retirement instead of hard deletion and replacement as retire + new record with a reusable display name; RouteTemplate presented as Planned Routes with archive-instead-of-delete for ever-used templates and no separate versioning; Machines, Route Templates, and PartNumber master metadata as permission-based production master data managed in Management (Machines, Planned Routes, Part Numbers), not Administration; plus the v10 rules: Area Completion — user-facing DONE, canonical `AREA_COMPLETED` Movement, derived `READY_TO_TRANSFER` holding state, quantity-scoped and distinct from `RELEASED_FROM_MACHINE` and `STOCKED`, with atomic implicit source completion during transfers and whole-command Undo; plus the v9 rules: Request Types `NEW`/`MODIFY` only; Repair as an explicit movement intent `TRANSFERRED · movement_reason REPAIR`; Floating Routes as the default route mode with optional AssignedRoute; nullable external Work Order Numbers rendered `—` — no temporary number generation; PN barcodes carrying the PN with create-on-first-use and case-insensitive identity; canonical `SCRAPPED` and auditable `QUANTITY_ADJUSTED · INCREASE` events; two Area ownership modes with one-shot Machine assignment and no Machine sessions; per-station Scan Station routing; administrative archival/purge maintenance policy; nullable due dates, canonical demand ordering, server-confirmed-write scan rule, confirmation before Hot reordering).
- Approved target UI: `GUI_DESIGN.md`, with the latest mockup (`docs/mockups/partflow-gui-mockup-v18.html`) as the visual reference.
- Phase 1 — Repository Foundation source code exists: React + TypeScript frontend, FastAPI backend, PostgreSQL, Alembic (no-op baseline), Docker Compose, the `/api/health` endpoint, formatter/linter/type-check/test tooling, and CI.
- Phase 2 — Frontend Design System and Application Shell is implemented: semantic design tokens with switchable Dark/Light themes (Dark default), URL-based routing and navigation (Management remembers its last-used sub view per session), all ten approved GUI views as mock views behind a real development-only build boundary (mock views and datasets are loaded only when `import.meta.env.DEV` is true; production builds render an explicit not-connected state per route, verified by a mock-sentinel check in `npm run build`), loading/empty/error/disconnected/long-data states with a dev-only `?state=` preview, and the real `/api/health` connectivity integration with **fast connectivity detection** (GUI_DESIGN §3.6): browser online/offline events, ~1 s health polling with a request timeout below the probe interval and no overlapping probes, recheck on tab focus/visibility, passive probes never flipping the UI to a "connecting" state, persistent OFFLINE banner with Retry, write controls disabled while disconnected, and Scan Station input re-enable/refocus on recovery — no WebSocket/SSE and no offline write queueing. The views implement the approved GUI v9 interaction model against local mock state: the Work Orders view (route `/management/work-orders`) with New Work Order as a modal dialog over the WO list, optional WO Number and due dates (a blank number is saved as NULL and renders `—` — no temporary number generation), the manual-first multi-step Add Part dialog with PN-carrying barcodes and create-on-first-use as a secondary method, the save-omission confirmation, OPEN Work Order demand-line adding/removal per the Work Order Demand removal rule (PROJECT_PROFILE §13), native calendar date inputs, mock validation, and unsaved-change protection; the Production Board with live header clock, the shared Hot presentation (`🔥#n` before the PN), urgency-text-only blinking, scrap visibility on the total line, Job-Numbers-last column order, and dynamic height-measured pagination; the Scan Station as a per-station route (`/scan-station/:stationId` behind a Station Selector) with PN-centric one-shot wizards that end in dedicated structured confirmation views and record nothing before the final confirmation (three-step intake with MODIFY/FLOATING defaults, source-explicit transfers, three-step one-shot Machine assignment with Step-1 barcode selection and no Machine session, auditable quantity addition, explicit Repair intent, the PF:SCRAP counting workflow, Undo with the shared summary-confirmation presentation), an ENTER-button-free Scan Barcode card with in-row manual PN entry, floating auto-dismissing scan notifications (the OFFLINE banner stays persistent), grid-layout PN rows with a separated action rail (`{n} scrapped` text only, `QUEUE` only on Machine cards), and Area statistics in the header; the Area Board per-Area detail and the Scan Station sharing one Area/Machine monitoring layout built from shared presentation components; and Priority Management with confirmation before every order change. The GUI v10 refinements are implemented against development-only mock state: the Area completion state (manual DONE from Machine-card rows — the distinct `DONE`/`QUEUE` actions — and from the direct-processing action dialog; the `Finished — ready to move` group; implicit `AREA_COMPLETED` + `TRANSFERRED` as one atomic mock command with whole-command Undo), keyboard-wedge scan capture at the Scan Station, the restructured header (identity + Worker Session row, Operations chips, reconciling semantic totals incl. Done), the statistics-free `In this Area now` card, the Production Board's explicit Area/Machine/state location model (compact Machine chips — with the state wording later expanded to the full `on machine` by GUI v13 — distinct `done` rows, separated scrap line, enforced PN minimum width, rebalanced column widths), the Area Board's shared-row All Areas overview with finished-state distinction, Tracking's sibling route arrows plus `AREA_COMPLETED` history and ready-to-transfer presentation, the Work Order Details modal dialog over the list with the component-owned QuantityKeypad stylesheet, the redesigned Priority ranking confirmation (moved-item summary, current-vs-proposed comparison, `Apply ranking`), the professional operator-copy pass with the standard `Cancel (Esc)` label, and a mock Scan Station state machine so confirmed commands update the monitoring surfaces. The GUI v11 refinements complete the Phase 2 presentation: the Scan Station production mode route `/scan-station/:stationId/production` (top application navigation hidden — presentation only, never a security boundary; standard mode unchanged for Manager/Admin use), Station Selector cards with Operation chips and the explicit `Open` / `Open production mode` actions, the Production Board's board-owned Live heartbeat, No.-column Hot flame, intrinsic 15ch PN width, `Location | Quantity | State/activity | Time` row alignment with full Machine names and External activity chips, the continuous total separator with the same-line scrap chip, manual page controls with ArrowLeft/ArrowRight, the Priority two-snapshot ranking confirmation (`Current Position` → `New Position`, explicit WO/Job metadata chips, `Not listed` add/remove placeholders), and the exhaustive rendered-copy audit with its automated guard (`src/rendered-copy.test.ts`). The GUI v12 refinements polish the Phase 2 presentation further: cursor-aware shared Quantity NumPad editing (one selection/caret model for the physical keyboard and the on-screen keypad), a shell layout that derives the content area from flex layout (`100dvh` + fallback) so fitting views show no false vertical scrollbar, the compact production-mode Dark/Light control in the Scan Station header-actions group, the dialog information hierarchy (distinct description / recap-with-entity-chips / marked guidance kinds / strongest validation) with the confirmation-summary row emphasis, the Production Board's coherent time-first clock, the subtle Hot-flame pulse with its reduced-motion fallback, rebalanced column shares with heading-derived Due Date / Total Days minimums, shared cross-row location tracks, the flex-anchored footer with the user-facing sorting legend (`Order: Hot rank first → earliest due date → no due date by oldest received date.`), and the Priority `#old → #new` rank transitions with explicit `Not listed` add/remove sides. The GUI v13 refinements continue the Phase 2 presentation polish: the Station Selector's full-card standard-mode selection with a separate `Production mode` action, the unified non-interactive Scan Station footer (Station ID, mode label, `Ctrl+Shift+K: switch mode` hint) in both modes, the secondary-toned `Total pcs` statistic, the vertical connectivity/theme stack with a borderless compact theme control in the production-mode header, touch-primary quantity inputs (`inputMode="none"` plus a `visualViewport` NumPad-collapse fallback), the Production Board's full `on machine` wording with semantic state-text tones and plain error-toned scrap text, the deadline-synchronized page-rotation indicator, the restrained footer aggregate summary, the addressable Production Board Kiosk mode (`/production-board/kiosk`, `Ctrl+Shift+K`, board-owned kiosk header, hidden application navigation), Tracking's toggleable/closable detail panel with a full-width no-selection state, the Work Orders copy cleanup and responsive demand-line card layout with near-full-screen narrow-viewport dialogs, the Priority confirmation's shared snapshot grid tracks and separated Action emphasis, and the honest Administration placeholders distinguishing the Phase 2 preview, the Phase 3.5 minimum environment setup, and full Administration (Phase 13). The GUI v14 changes continue the Phase 2 presentation: the two new Management sub views as mocks — Machines (`/management/machines`: active-Machines table with derived operational state and elapsed time in state from `stateChangedAt`, maintenance override with note/expected return, retirement blocked while quantity is assigned, the Retired Machines table (action-less until GUI v15), the New/Edit Machine dialog with a fixed Area for existing Machines and optional asset metadata) and Planned Routes (`/management/planned-routes`: searchable RouteTemplate list with Active/Archived status, usage dialog, future-assignments-only edit note, archive-instead-of-delete for ever-used templates, delete for never-used only) — making the Management sub-view order Area Board · Machines · Tracking · Work Orders · Planned Routes · Priority (both new views permission-based production master data, deliberately not Administration, which keeps no duplicate screens); the shared `pf-heartbeat` connected-dot animation behind the `ONLINE` chip and the Production Board `Live` status; the kiosk header showing the same `Live` status with an explicit `Exit kiosk` button instead of the ONLINE chip; the ~20 % tighter Production Board location-grid track minimums; Tracking's whole-row selection with the modeless floating detail overlay (no table reflow on open/close); the content-sized Priority snapshot rank track; the Scan Station's `inputMode="none"` main barcode input on touch-primary devices via the shared `touch-device.ts` module, the header-card-language actions group, the content-sized flattened confirmation summaries, the operator-facing intake copy, and the `Assign to Machine` dialog naming; the shared monitoring cards' state age (`running · 1h 24m`) and maintenance context; and the Administration sidebar without Machines/Route Templates sections. The GUI v15 changes (later superseded as current by GUI v16–v18 and post-v18 refinements — GUI_DESIGN.md §15, PROJECT_PROFILE.md v17 — including the Part Numbers management view and its route `/management/part-numbers`, and the Manager Summary view merging into Area Board's All Areas overview): the Machine return-to-service lifecycle (PROJECT_PROFILE v12) as mock behavior — the Machines view's `Machine | State | Assigned now | Asset | Maintenance` columns with the per-row Maintenance On/Off switch (opening the existing dialogs) and whole-row Edit activation, the Edit dialog's append-only lifecycle audit list and Danger-Zone Retire with a typed identifier confirmation (Asset Tag, else barcode), and the Retired Machines table's per-row `Reactivate…` dialog (same-physical-machine checkbox, required reason, identity-reissue blockers, active-name-uniqueness rename, forward-only Area change, returning Idle, appending `REACTIVATED`); the shared `TypedConfirmDialog` / `UnsavedChoiceDialog` primitives with `Cancel (Esc)` compliance in the Machines/Routes dialogs; the Planned Routes active/archived table split with Area-colored step chips, whole-row editing, in-dialog Duplicate/Archive…/Delete…, drag-and-drop reordering with kept Up/Down controls, Area-scoped Operation selects and preferred-Machine selects by stable Machine id (`preferredMachineId` matching `preferred_machine_id`; explicit `(unavailable)` values, never silently cleared), and the typed archive-confirmation flow; the four-kind guidance taxonomy (marker-less neutral removed), the restructured intake steps, semantic confirmation-summary value tones with Area dots, operator-only scrap copy, and the sharpened offline copy family; the `.ss-headgroup` production-mode header; the Production Board's proportional, configurable page rotation (`ROTATE_MS_PER_ROW` / `ROTATE_MS_MIN` — 3 s per displayed row, 6 s minimum — with the indicator sharing deadline and duration), the unified three-zone kiosk header with the `Exit kiosk` control moved to the footer controls row, and the neutral total-quantity tones; the full-width Work Orders list with one search/New toolbar row, whole-row activation and dialog heading polish; and the Priority snapshot divider between the shared position track and the PN column. The canonical Purchase Order → Work Order vocabulary migration (PROJECT_PROFILE v7, GUI_DESIGN §15.10) is applied across current documentation, frontend code, routes, and tests. Phase 2 remains frontend presentation with development-only mock behavior: it contains no domain implementation, no backend business APIs, and no persisted production writes — every Phase 2 save changes development-only mock state.
- Phase 3 — Minimum Canonical Domain and Data Foundation is implemented: the framework-independent domain vocabulary in `backend/app/domain/` (canonical PN normalization — trim surrounding whitespace, reject internal whitespace, canonicalize to UPPERCASE, with `InvalidPartNumberError` — and the `RequestType` (`NEW`/`MODIFY`), `RouteMode` (`FLOATING`/`PLANNED`), `MovementType` (`RECEIVED` only) and `QuantityFlowStatus` (`ACTIVE` only) enumerations, each widening additively in the phase that introduces new values), the SQLAlchemy schema mappings in `backend/app/infrastructure/models.py`, and the Alembic migration `0002_phase3_domain` creating the twelve canonical tables with explicit deterministic constraint and index names: `departments`, `areas`, `operations`; the optional `part_numbers` master with the canonical PN string as its natural primary key (canonical-form CHECK, no surrogate id, and **no foreign key from any production table** — the master stays hard-deletable per PROJECT_PROFILE §8.1/§28); `work_orders` with a nullable external Work Order Number and a partial unique index over non-null numbers only; `work_order_demands` (canonical-PN CHECK, `request_type IN ('NEW','MODIFY')`, positive requested quantity, nullable due date, `priority_rank`, Job Numbers as a plain text array — metadata, never an aggregate); `route_templates`/`route_steps` (no version column — snapshots preserve history); the independent `assigned_routes`/`assigned_route_steps` snapshot tables (no `quantity_flow_id` back-reference); `quantity_flows` with `route_mode` (`FLOATING` default / `PLANNED`), `assigned_route_id` unique and present exactly when `PLANNED` (CHECK), the `current_area_id NOT NULL` current-position projection column, and the `(id, part_number)` composite-FK target; and the append-only `part_movements` event table (movement-type CHECK currently `RECEIVED` only, `RECEIVED` shape check `from_area_id IS NULL`, positive quantity, unique `device_event_id` idempotency key, composite FK `(quantity_flow_id, part_number)` → `quantity_flows (id, part_number)` so a Movement can never carry another flow's PN, and a statement-level raise-on-write trigger rejecting UPDATE, DELETE, and TRUNCATE in PostgreSQL itself). Constraint-level integration tests (`backend/tests/test_phase3_schema.py`) run the real migrations upgrade → downgrade → upgrade against dedicated temporary databases and verify these invariants, models↔migration metadata parity, and the absence of every deferred later-phase column and table; `backend/tests/test_part_number_normalization.py` covers the canonical PN rules. Phase 3 is domain and data foundation only: **no business API, no intake or release workflow, and no production write path exists yet** — the schema is migrated but nothing writes production data. The generic `audit_events` table belongs to the Phase 4 slice migration (SLICE1_DATA_MODEL §16–§17); `scan_stations`, `machines`, and the other environment configuration arrive with Phase 3.5; the additional application-role UPDATE/DELETE revocation on `part_movements` arrives with deployment hardening once a distinct application database role exists (the trigger already enforces append-only for every non-superuser path).
- Phase 3.5 — Minimum Environment Setup is implemented end to end (persistence, APIs, and the real frontend wiring). Persistence: the Alembic migration `0003_phase35_environment` extends `areas` (description, color, terminal flag, the server-assigned stable `PF:AREA:<id>` barcode guarded by an assign-once trigger) and `operations` (description, optional expected duration, external flag), and creates `scan_stations` (stable URL-safe Station ID as the natural key, Area binding, active flag — no station barcode), `machines` (immutable auto-assigned Asset Tags guarded by triggers, per-Area active display-name uniqueness, the explicit maintenance override, `state_changed_at`, retirement dates), the append-only `machine_lifecycle_events` history (`RETIRED`/`REACTIVATED` with nullable reference-free actor, reason, before/after state, and the previous → current Area pair on a move while retired), and the singleton Machine Asset Tag format configuration with its persisted never-reuse counter. APIs: the environment configuration surface (`/api/departments`, `/api/areas`, `/api/operations`, `/api/scan-stations`, and `GET`/`PUT /api/barcode-configuration/machine-asset-tag-format` — create/read/update and deactivate only, no DELETE endpoints, server-owned fields rejected via `extra="forbid"`) and the Machines management surface (`/api/machines` — listing, creation with server-side Asset Tag allocation taking the previewed `expected_asset_tag` as an optimistic precondition only, the single Save-changes PATCH transaction covering metadata plus the in-place maintenance context, the maintenance override start/clear sub-resource, and retirement/reactivation each committing atomically with its lifecycle event, a retirement optionally carrying the recorded Save decision of the Edit dialog's unsaved edits), with Application-layer services owning every rule and transaction and typed errors mapped centrally to HTTP responses. Frontend: Administration's five minimum-environment sections (Departments, Areas, Operations, Scan Stations, Barcode configuration) and Management → Machines are REAL views reading and writing this API — the approved GUI v18 interaction model unchanged, mock state replaced by server state with explicit loading/error/retry presentation, server-rejected writes surfaced in place, and the offline write-block still gating every write action. The production build boundary was restructured for this: the real views live in `src/app/real-views.ts` and ship in every build alongside the shared API client layer (`src/api/`), while the remaining Phase 2 mock views stay behind the development-only registry (`src/app/dev-views.ts`) — `src/mocks/` never enters a production bundle (sentinel-scan on build plus a source-level no-mock-import guard over the production modules in `src/production-boundary.test.ts`); the development-only Worker sessions policy preview is reachable solely through an `import.meta.env.DEV`-guarded lazy import, and every not-yet-real Administration section presents itself honestly as arriving with full Administration (Phase 13). Still deliberately absent (per this phase's scope): no production workflow and no production write of any kind, no Machine assignments (Running/assigned-quantity presentation activates with the Phase 6 workflows — until then active Machines are Idle unless Maintenance overrides), no real Planned Routes/Part Numbers/Workers/Users/Worker-session policies, no `audit_events`, and per-Area Worker ID modes arrive with the Worker-session workflows (the Areas table shows the column unconfigured).
- Phase 4 — Manual Work Order Intake and Production Release is implemented end to end. Persistence: the Alembic migration `0004_phase4_audit` adds only the generic append-only `audit_events` table (SLICE1_DATA_MODEL §16–§17 — `CREATED`/`UPDATED` events for `WorkOrder`, `WorkOrderDemand`, and `PartNumber` only; polymorphic `entity_id` with no foreign key, nullable `actor_reference` until authentication exists, `before_data`/`after_data` jsonb snapshots, and a raise-on-write trigger enforcing append-only in PostgreSQL itself; as for `part_movements`, the additional application-role UPDATE/DELETE revocation of SLICE1_DATA_MODEL §16–§17 arrives with deployment hardening once a distinct application database role exists — the trigger already enforces append-only for every non-superuser path). Every WorkOrder/WorkOrderDemand creation or edit and every PartNumber-master creation commits in the same transaction as its audit rows; production release writes **no** generic audit row — the immutable `RECEIVED` Movement is the production audit record. APIs: `/api/work-orders` (create/find with verbatim non-null Work Order Numbers and a confirmed blank saved as `NULL` on an internal Work Order; nullable WorkOrder and demand due dates as valid data; list/search; a single-transaction PATCH for the audited header/number edit and diff-only demand-line edits plus new lines with canonical-PN normalization and PartNumber-master create-on-first-use; DELETE of a demand line enforcing the removal rule transactionally — refused once production quantity has been released, never cascading to the PN master, QuantityFlows, Movements, release history, or other demand, and the last remaining line never removable), `/api/part-numbers` (lookup/search and create-or-reuse on the canonical PN, with the derived `PF:PN:<canonical-part-number>` barcode in responses — the minimal label capability this phase owns; no update and no DELETE, full master management stays Phase 13), the read-only `GET /api/route-templates` listing that a `PLANNED` release selects from (no Planned Routes management — Phase 13), and the explicit release command `POST /api/work-orders/{work_order_id}/demands/{demand_id}/release` — initiated from a saved WorkOrderDemand line (the demand context travels informationally in the Movement metadata; WorkOrderDemand never owns Movement) — which validates the canonical PN, the starting Area (active **and non-terminal** — a terminal Area is where finished quantity ends, never where production enters, so the rule lives in the Application layer and not only in the release dialog's Area list), the Operation, an active RouteTemplate when `PLANNED`, and a positive quantity within the demand's remaining quantity; requires the explicit confirmation flag when the PN already has active quantity (showing the existing distribution — never auto-creating or auto-merging); snapshots an independent AssignedRoute for a `PLANNED` release only (a `FLOATING` release has none; later template edits never alter a snapshot); creates the QuantityFlow with its route mode and `current_area_id` set by the INSERT itself; appends the immutable `RECEIVED` Movement; and commits all of it atomically. `device_event_id` idempotency stores a deterministic request fingerprint in the Movement metadata: the same id with the same fingerprint replays the original committed result, a mismatched reuse is an explicit idempotency conflict, and both create nothing; the current-position projection remains rebuildable from Movement history alone (replay verified by tests). Released state is server-derived from the immutable `RECEIVED` metadata context, with no stored counter and no migration: demand lines carry `has_released_quantity` plus their `released_quantity` and `remaining_quantity`, and a Work Order derives `RELEASED` only when every current line is fully released — a partly released line keeps it `OPEN`, and the stored status column remains `OPEN` throughout. A demand may therefore be released in **several parts** (20 of 50, then 12, then 18): each part is its own separate QuantityFlow behind the active-quantity confirmation, the remaining quantity is the hard server-side cap (a release beyond it is refused and creates nothing), and a fully released line stops offering the action. A line with any released quantity keeps only the **restricted edit** of PROJECT_PROFILE §13, enforced in the backend and not merely in the UI: `requested_quantity` (never below `max(released, allocated)` — raising it restores remaining quantity and the Work Order's derived `OPEN`), `due_date` and `job_numbers` save with their normal `UPDATED` audit row, while `request_type`, `requester`, `reason` and `notes` are refused with a 409 that writes nothing at all — not even the valid fields travelling with them — and removal stays refused; the audited Work Order header/number edit stays available (a header-only save takes no demand lock at all). The edit path locks each edited line `FOR UPDATE` and recomputes its released quantity under that lock, so a demand edit and a release of the same line serialize in either arrival order and `released_quantity > requested_quantity` is unreachable. The Add Part PN lookup is bounded server-side (at most 50 masters per listing, unchanged canonical order); the exact `number=` resolution is never bounded away, so a short but valid canonical PN still resolves exactly. Frontend: Management → Work Orders is a REAL view on the live `/api` surface (GUI_DESIGN §11 — list/search, New Work Order resolving an entered existing number instead of duplicating, Work Order Details with diff-only Save demand and draft isolation for server actions — the unsaved-changes state is the real draft-versus-saved difference, saved-line removal waits for it to be settled, and Release opens the shared `Unsaved changes` Save/Discard/Cancel decision instead of being blocked, always running on the committed demand — the §11.4 release dialog with retry-safe `device_event_id` reuse and the active-quantity confirmation flow, released lines restricted from load — Qty (flagged red at entry time when it falls below the committed quantity), due date and Job Numbers editable on an Open and on a Released Work Order alike, PN and Request Type read-only, removal blocked — while their remaining quantity stays releasable, a changed release intent taking a fresh `device_event_id`, the audited external-number edit on internal Work Orders, and the printable derived `PF:PN:` Code128 label from intake, opened from the demand-line PN itself through the ONE shared `Part Number barcode label` dialog — a provisional entry point that retargets to `Edit Part Number` in Phase 13), while the Completed Work Orders page stays an honest production placeholder (completion is derived from allocation — Phase 10) with its approved presentation behind the development-only build boundary. Phase 4 hardening (post-implementation, after the independent audits): the "one canonical PN at most once per Work Order" rule is serialized where it is written — a save that adds demand lines takes the parent Work Order row lock and re-reads that Work Order's canonical PN set under it, so two concurrent saves adding the same PN can never both create a line (lock order stays demand ascending → Work Order, matching demand removal); the active Work Order list is bounded server-side at 100 rows with `search` evaluated in the database, because nothing leaves the active list before allocation-derived completion (Phase 10) while the exact `number=` resolution stays unbounded so a duplicate number can never hide behind the bound; and the migration `0005_phase4_release_index` adds one partial expression index over `part_movements` matching the released-quantity derivation exactly (`(metadata['context'] ->> 'work_order_demand_id')::int WHERE movement_type = 'RECEIVED'`), which keeps the Phase 4 hot read path off a full scan of the append-only Movement history without adding a column, a foreign key, or a stored counter. Still deliberately absent (per this phase's scope): no Scan Station transfer and no Phase 5+ movement type (the movement-type check still admits `RECEIVED` only), no Machine assignment, no SPLIT/MERGED, no Undo/corrections, no Stockroom/allocation/completed history, no file-based import, and no real Planned Routes or full Part Numbers management (Phase 13).
- Phase 5 — Scan Station Transfer to an Area Queue is implemented end to end (persistence, the Application command, the Scan Station read models, and the real Scan Station frontend on this API). Persistence: the Alembic migration `0006_phase5_transfer` widens `part_movements` only — the movement-type check admits `RECEIVED` and `TRANSFERRED`, the nullable `part_movements.station_id` (FK to `scan_stations.station_id`, SLICE1_DATA_MODEL §18) records the stable Scan Station identity of a scan-driven Movement for audit (NULL on the Management-initiated `RECEIVED`), and the single-type `received_shape` check becomes the per-type `movement_shape` check (`RECEIVED` has no source Area; `TRANSFERRED` moves between two different Areas and always carries its Station). No table is added, and the append-only trigger covers `TRANSFERRED` rows unchanged. APIs: `GET /api/scan-stations/{station_id}/context` (the bound Area with Department and color, its active Operations, and whether it has Machines — an unknown station is 404 and an inactive station or bound Area 409, never a silent substitute), `POST /api/scan-stations/{station_id}/scans/resolve` (a `PF:PN:` barcode with the exact prefix, or a manual PN entry, canonicalized by the one domain rule — raw PN text and other namespaces are rejected — resolved into the PN's quantity already in the station's Area and the explicit **transfer candidates** elsewhere: each ACTIVE flow outside the Area judged by its current position and route — `FLOATING`, `ON_ROUTE` when the station's Area is the flow's next expected snapshot step, `DEVIATION` otherwise — with the Operation the destination resolves to, the initiating Work Order context for the recap line, and whether active demand exists; several candidates are returned as they are, never ranked or combined — the client must make the operator select exactly one), `POST /api/scan-stations/{station_id}/transfers` (the confirmed move of ONE whole QuantityFlow — chosen by id with the source Area the operator saw it in AND the destination Area the operator resolved and confirmed as optimistic preconditions — into the station's Area: the Scan Station row is locked until COMMIT and must still be active and bound to exactly the confirmed Area (a station deactivated or rebound since the confirmation is refused with nothing written — the UI can never confirm Area A while the backend records Area B), then validated against Area activity, the terminal flag, PN agreement, flow status and current position, then the destination Operation resolved from the Area's configuration — the matched route step's Operation, else the single active Operation, else an explicit choice, several without a choice being an ambiguity that blocks the write — locked `FOR UPDATE` and re-validated under that lock so transfer versus Operation deactivation has one serial outcome, and the immutable `TRANSFERRED` Movement appended with `from_area_id`, `to_area_id`, `operation_id`, `station_id` and the matched immutable snapshot step while `quantity_flows.current_area_id` is updated in the same transaction; 201 fresh, 200 on an idempotent replay of the same `device_event_id` + same confirmed intent (the request fingerprint covers station, flow, PN, source and destination Area, quantity, Operation choice and deviation reason) — checked BEFORE any station/Area/Operation state is read, so the replay holds after the flow moved on, after the station was deactivated or rebound, and when the race is lost at COMMIT — 409 on a mismatched reuse; the source flow row is locked `FOR UPDATE` so concurrent transfers of one flow serialize with exactly one winner (lock order flow → station → target Area → Operation), and the target Area row is locked until COMMIT — its active and terminal flags are judged on that locked re-read, never on the earlier unlocked station read — so transfer versus Area deactivation (or a terminal flag set meanwhile) has one serial outcome), and `GET /api/areas/{area_id}/inventory` (the ACTIVE quantity currently in an Area grouped per PN — the refresh source after a transfer). Route deviation (PROJECT_PROFILE §17) on a `PLANNED` flow covers the route expectation as a whole — the next step's Area AND, when the step defines one, its Operation: a different Area (`kind: AREA`) or a different active Operation at the expected Area (`kind: OPERATION`) is warned and refused until the request carries the explicit `confirm_route_deviation` flag together with a mandatory `route_deviation_reason` (§17 step 7 — confirming without a reason is refused with nothing written; the 409 body carries the expected step/Area/Operation versus the actual Area/Operation); a confirmed deviation is recorded ON the `TRANSFERRED` Movement (metadata `route_deviation` with kind, expectation, actuals, reason and the confirmation; NO snapshot step — a step is never recorded as matched under another Operation; the previous route untouched and the last known route position preserved) — no `ROUTE_DEVIATION_CONFIRMED`/`ROUTE_ADJUSTED` Movement and no route edit exist yet (authorized AssignedRoute adjustment is owned by Phase 14). The projection stays rebuildable from Movement history alone (replay verified). Temporary limitations of this phase as it shipped, enforced server-side (the whole-QuantityFlow limitation was lifted by Phase 8): **whole-QuantityFlow movement only** — a smaller quantity was refused with a clear message and no write (SPLIT, Phase 8), a larger one exceeds the source; and a transfer records `TRANSFERRED` alone (no `AREA_COMPLETED` before Phase 6/7). Still deliberately absent as Phase 5 shipped (Machine assignment and `AREA_COMPLETED` arrived with Phase 6, SPLIT/MERGED with Phase 8): Worker/Machine barcode resolution and Worker sessions, Machine assignment, `AREA_COMPLETED`, SPLIT/MERGED, Repair/Scrap/Undo, the Stockroom `STOCKED` (a terminal Area is never a transfer target), the Receive Quantity intake from the station, and processing states (queued/on machine/done) — Phase 5 quantity in an Area is simply "in this Area". Frontend: the Station Selector and the Scan Station are REAL views on the live `/api` surface (GUI_DESIGN §4.11 implementation boundary) — `src/api/scan-station.ts` plus `views/scan-station/ScanStationView.tsx`, registered in `src/app/real-views.ts` and shipping in every build: the selector lists the active stations from the environment configuration; a station loads its context, Machines and live Area inventory; scans and manual entries resolve on the server; the §4.7 transfer wizard (explicit source selection with several sources never auto-selected, destination Operation resolution, whole-flow quantity with partial input refused and never submitted (until Phase 8), the confirmation summary, one `device_event_id` per intent reused on retry, success only after the server confirmed, then inventory refresh, Last Action and focus restoration) is the one production command; Machine/Area/scrap barcodes, Worker identity and every Phase 6+ action are honest placeholders that record nothing. The former mock Scan Station (`ScanStationMockView.tsx`, the approved Phase 6+ workflows) left the development-only registry and survives only as a DEV preview behind the real view's `import.meta.env.DEV` boundary (`?preview=mock`), with the shared mock-free presentation primitives extracted to `scan-station-presentation.tsx` / `scan-station-wizard.ts`; the production-boundary suite verifies the mock view and its Area state never enter the production module graph.
- Phase 6 — One-Shot Machine Assignment and Area Completion is implemented end to end (persistence, the Application commands, the Scan Station read models, and the real Scan Station frontend on this API). Persistence: the Alembic migration `0007_phase6_machine_assignment` adds `quantity_flows.current_machine_id`, the Movement Machine references `part_movements.source_machine_id` / `destination_machine_id` and `part_movements.command_sequence` — one `device_event_id` identifies one application command that may append several Movements (`UNIQUE (device_event_id, command_sequence)`) — and admits `ASSIGNED_TO_MACHINE`, `RELEASED_FROM_MACHINE` and `AREA_COMPLETED` with a per-type shape check (the in-Area types stay in one Area at a Scan Station with exactly their Machine reference). Application: the holding state is derived from the flow's latest Movement (`QUEUED` / `ON_MACHINE` / `READY_TO_TRANSFER`), the three Machine-Area commands (whole-QuantityFlow as this phase shipped, partial since Phase 8) (`POST /api/scan-stations/{id}/machine-assignments` | `machine-releases` (QUEUE) | `area-completions` (DONE) — 201 fresh / 200 idempotent replay / 409 mismatched reuse, also across command kinds and against the Phase 4 release ids) validate the Machine under its row lock (active, not under maintenance for assignment, the station's Area; several PNs per Machine; nothing ever auto-assigned), a transfer of `ON_MACHINE` quantity appends `AREA_COMPLETED` + `TRANSFERRED` as one atomic command, the Machine operational state and `state_changed_at` stay derived and move only on a real state change, and retirement is refused while quantity is assigned. Read models: `POST /api/scan-stations/{id}/machine-scans/resolve` (the one-shot Machine-first context, nothing stored), `available_actions` / `requires_selection` per flow, and the inventory split `queued` / `machines` (ON_MACHINE only) / `finished`. Frontend: the real Scan Station renders the split, resolves Machine barcodes into `Assign to Machine` with the Machine preselected, offers PN-first `Assign to Machine` and `Complete Area processing on {Machine}`, carries the distinct Machine-card DONE / QUEUE actions through quantity → summary → final question, announces the implicit completion on transfer, reports success only after the server confirmed the write (unknown outcome frozen and retried under the same `device_event_id`; a server refusal re-reads the Area and withdraws Back, leaving only Retry or Cancel), and Management → Machines shows the server-derived state and assigned total. Still absent as Phase 6 shipped: direct Area processing (arrived with Phase 7), SPLIT/MERGED (arrived with Phase 8), Worker identity and badge gates, Undo, Repair, Scrap, Stockroom. The Phase 6 section below records the detailed implementation state.
- Phase 7 — Direct Area Processing (Areas Without Machines) is implemented end to end (persistence, the Application command, the transfer widening, the Scan Station read models, and the real Scan Station frontend on this API). Persistence: the Alembic migration `0008_phase7_direct_processing` widens only the `AREA_COMPLETED` branch of the per-type Movement shape check so a completion may carry no source Machine (never a destination Machine) — no column, no table, no new movement type and no Area configuration column: exactly two Area modes exist and the mode follows from the Area's active Machines (`app.application.machines.area_has_machines`). Application: the holding state is derived from the flow's latest Movement AND the Area's mode (`ProcessingState.PROCESSING` — an arriving `RECEIVED`/`TRANSFERRED` in an Area without Machines; QUEUED in a Machine Area exactly as before), so an Area without Machines takes direct processing ownership with no queue, Machine NULL and the Operation recorded on the arrival (several active Operations need the explicit choice — nothing is picked); the manual DONE without a Machine (`app/application/direct_processing.py`, `POST /api/scan-stations/{id}/area-completions` without `machine_id`) appends exactly one immutable `AREA_COMPLETED` with `source_machine_id` NULL and derives READY_TO_TRANSFER (refused with zero writes in a Machine Area, as the Machine-Area DONE / QUEUE / ASSIGN are refused on directly processing quantity); a transfer of PROCESSING quantity appends `AREA_COMPLETED` (no Machine) + `TRANSFERRED` as one atomic command under one `device_event_id`, READY_TO_TRANSFER quantity transfers with `TRANSFERRED` alone, and the projection replay reconstructs PROCESSING → READY_TO_TRANSFER → transferred from history. Idempotency (the direct DONE and the Machine DONE are distinct intents under one id), the single transaction per command, the flow/station row locks and the whole-QuantityFlow limitation are unchanged from Phases 5–6. Read models: `has_machines` on the station context and the Area inventory, `available_actions` PROCESSING → DONE, TRANSFER, and the inventory split `processing` / `finished` for an Area without Machines (no Machine cards, structurally zero queued / on-Machine figures). Frontend: the real Scan Station renders the server's Area mode and states (the `In processing` / `External processing` rows by the flow's recorded Operation, `Finished — ready to move`, no queue and no Machine cards in an Area without Machines), carries the single `DONE` row action on exactly the actively processing rows and the PN-first `Complete Area processing` choice, opens the same Area Completion wizard without a Machine field (Operation named on the summary, final question, success only after the server confirmed the Machine-less `AREA_COMPLETED`, then the Area re-read and focus restored), and announces the implicit completion of directly processing quantity on transfer (`Recorded events: AREA_COMPLETED, then TRANSFERRED`) while finished quantity transfers with `TRANSFERRED` alone. Still absent: Worker identity and badge gates, Undo, Repair, Scrap, Stockroom. The Phase 7 section below records the detailed implementation state.
- Phase 8 — Quantity SPLIT and MERGED Workflows is implemented end to end (persistence and lineage, partial quantity on every Scan Station command, the explicit merge, the read models and the projection replay, and the real Scan Station frontend: partial quantity on every wizard and the operator-facing `Combine quantities` workflow). Persistence: migration `0009_phase8_split_merge` widens the movement-type check with `SPLIT` and `MERGED` (one shared shape branch: ONE Area at a Station, NO Machine), constrains the Quantity Flow lifecycle (`status` ACTIVE / SPLIT / MERGED, `closed_at` set exactly for a closed flow) and adds the append-only `quantity_flow_lineage` edge table (parent → child, relation, the command's `device_event_id`) — one edge per child for a SPLIT (1 → N), one per consumed source for a MERGED (N → 1), which is why no single `quantity_flows.parent_flow_id` exists. A lineage event is not a position: the holding state, Machine and Operation of a child or merge result derive by following its lineage to the last position-bearing Movement (`projections.effective_latest_movement`), and consumed flows are exactly the parents in the graph. Commands: every in-Area and transfer command accepts a quantity smaller than the flow's — the source splits atomically inside the same command (source SPLIT, selected SPLIT, remainder SPLIT, then the action rows, all under one `device_event_id`; a PLANNED source gives each child its own snapshot copy at the source's route position and the action records the child's step or deviation), the action applies to the selected child only, the remainder keeps the source's state, the whole quantity never splits; `POST /api/scan-stations/{id}/merges` merges named ACTIVE flows of one PN in the station's Area only when their production context is identical (state, Machine, Operation, route context) — anything else is refused with zero writes, nothing is ever merged automatically; the PN resolution reports `combine_groups` (the in-Area flows the server judges combinable by the same rule) so the station offers the action for exactly those. Frontend (GUI_DESIGN §4.7 item 3, §4.8, §4.11 Phase 8 boundary): every wizard accepts 1..MAX and shows the chosen part and the remainder before and after the write, submitting only the chosen quantity; the PN action dialog offers `Combine quantities` for the server-reported groups, with source selection, a result preview, the `Confirm combine` summary and the unchanged one-shot write model. The Phase 8 section below records the detailed implementation state.
- Phase 9 — Undo, Corrections, and Auditable Quantity Events is implemented end to end (persistence, the reversal-aware derivation, the command-level Undo with its preview read model, Repair as the explicit intent on the transfer command, Scrap, quantity additions, the Scan Station read-model widening — `repair_available`, `scrapped_quantity`, `SCRAP` in `available_actions` — and the real Scan Station frontend for all four correction workflows on this API). Persistence: migration `0010_phase9_undo_corrections` — the movement-type check admits `SCRAPPED` / `QUANTITY_ADJUSTED` / `REVERSED` with their shape branches, `part_movements` gains `movement_reason` (only `REPAIR`, only on `TRANSFERRED`), the mandatory free-text `reason` (Scrap / adjustments / Repair, CHECK-enforced) and `reverses_movement_id` (FK, set exactly on a `REVERSED`, UNIQUE — a Movement can never be reversed twice, even by a race), and `quantity_flows.status` widens with `SCRAPPED` / `REVERSED`. Undo (`POST /api/scan-stations/{id}/undos` + the `undo-preview` read model) reverses the COMPLETE application command — the multi-Movement transfer, SPLIT-prefixed partials and merges as one — by appending compensating `REVERSED` rows in reverse order while every derivation excludes the reversed pair, so the restored state (position, Machine, route position, holding state, reopened/closed flows, Machine totals) is exactly the state before the undone command, projection and replay agreeing by construction; eligibility (most recent effective operation, the recording station only, never Management events, never twice, never an Undo of an Undo, no restore onto a retired Machine or into a deactivated Area) is judged under the row locks with zero writes on refusal, and authorization / reason-when-configured stay deliberately absent until Phases 13/14. Repair is `TRANSFERRED · movement_reason REPAIR` with its mandatory reason on the same transfer command (previously visited destinations only, partial via SPLIT, deviations still confirmed separately); Scrap appends ONE auditable `SCRAPPED` per confirmation and closes the flow or split-off part (`introduced = active + scrapped` reconciles, scrapped totals net of reversed scraps); additions introduce a NEW FLOATING flow through `QUANTITY_ADJUSTED · INCREASE` beside existing in-Area quantity only, never touching requested quantities. Frontend: the real Scan Station records all four correction workflows — `Add more quantity` (no MAX, no default, mandatory reason, `QUANTITY_ADJUSTED · INCREASE` on the confirmation), `Return quantity for repair` (offered only for server-marked `repair_available` sources — also, through an explicit Transfer-or-Repair intent choice, when the PN has no in-Area quantity — explicit source selection narrowed to the eligible sources, mandatory reason, partial via the server SPLIT, the plain transfer with every valid source untouched), the §4.9 Scrap workflow (PF:SCRAP counts only inside it — the main input keeps rejecting it — pending count with −1/reset, one common reason, ONE server `SCRAPPED` operation per confirmation) and Undo from the Last Scanned PN block (targeting the most recent ELIGIBLE completed PN operation — the completed-command stack is walked newest-first through the server's `undo-preview`, the authority on eligibility; the structured summary and a warning-toned final question gate the write, and the stack advances to the next eligible previous operation after a confirmed reversal), all under the shared one-shot write model (`device_event_id` reuse, unknown-outcome retry, server-refusal re-read, inventory refresh and focus restoration). The Phase 9 section below records the detailed implementation state.

- Phase 10 — Stockroom and WorkOrderAllocation is implemented end to end: the backend (persistence, the `STOCKED` arrival command, the allocation record with its suggestion / confirmation / reversal commands, Work Order completion derived from allocation, the completed history read model, and the API) and the frontend (the Stockroom station's `Receive into Stockroom` workflow with the receiving allocation dialog of GUI_DESIGN §10 on the Scan Station shell, and the real Completed Work Orders page of §11.5 on `GET /api/work-orders/completed`). Persistence: migration `0011_phase10_stock_allocation` widens the movement-type and shape checks with `STOCKED` (the shape of a `TRANSFERRED`: two different Areas at a Station, no Machine), widens `quantity_flows.status` with `STOCKED`, adds `work_orders.completed_at` with its partial keyset index, and creates the append-only `work_order_allocations` table (allocation and reversal rows; canonical PN by value; the demand FK; `reverses_allocation_id` UNIQUE; the `device_event_id` + `command_sequence` idempotency pair; a raise-on-write trigger) — allocation never references a Movement or a QuantityFlow. `STOCKED` (`POST /api/scan-stations/{id}/stockings`) is the transfer's own command mechanism recorded at a station bound to a terminal Area (`transfers.record_arrival`): explicit source and confirmed destination, the established lock order, the route assessment and deviation confirmation, partial quantity through the in-command SPLIT (no parallel quantity mechanism), the implicit `AREA_COMPLETED`, whole-command idempotency and zero writes on refusal are unchanged; the flow closes as manufacturing-complete (`status = STOCKED`), leaves every read model and the projection replay, and — PROJECT_PROFILE §32 open decision 1 — is never undone (Undo refuses a `STOCK` command; allocation adjustment is the correction path). Available stocked quantity is derived (`Σ effective STOCKED − Σ active allocation rows`), never a stored counter; `work_order_demands.allocated_quantity` and `work_orders.completed_at` are maintained projections rebuildable from the allocation rows. `GET /api/allocations/suggestion` proposes in the canonical demand ordering (PROJECT_PROFILE §18 — Hot rank, dated earliest first, undated by received date — the received date orders undated demand only —, demand id as the deterministic tie-breaker, also between dated lines sharing a due date) up to each line's remaining shortage and never beyond the available quantity; `POST /api/allocations` (the receiving confirmation with the station, or a Management allocation) names the explicit `allocation_quantity` being allocated — the lines must sum to exactly it, it is part of the idempotency fingerprint, and it is an optimistic precondition the derived available stocked quantity must still cover under the lock (a stale figure is refused with nothing written) — and judges both invariants under a per-PN advisory lock plus the demand and Work Order row locks — a line never beyond its shortage, the allocation quantity never beyond the available stocked quantity — flags the operator's adjustments as overrides, and derives completion (every line fully allocated → `completed_at` set by that event, the Work Order leaving the active list as read-only history where edits, removal and release are refused; the exact `number=` resolution still finds it); `POST /api/allocations/{id}/reversals` is the auditable adjustment (once per allocation, mandatory reason, the Work Order reopened); `GET /api/work-orders/completed` is the server-side history (search over WO Number / PN / Job Number, done range, due outcome, the sort column and direction — Done descending by default —, keyset paging bound to that order, the matching total; the done date and the due outcome are judged on the site calendar, `SITE_TIMEZONE`). The adjustment capability ships here; its role-based authorization (who may adjust) is Phase 14 — no authentication or role check exists or is simulated before it — as Worker identity is Phase 13 and the monitoring read models Phase 11. Frontend: a station bound to a terminal Area opens the Stockroom receiving workflow instead of the ordinary transfer (`stock_available` from the server's PN resolution; the explicit source selection kept when several sources exist; quantity 1..MAX with the partial stocking split by the server inside the same command; `Confirm stocking` the only write point), and after the server confirmed the `STOCKED` write the allocation dialog loads `GET /api/allocations/suggestion?quantity=<stocked>` and renders every outstanding line in the canonical order (Work Order, requested, previously allocated, remaining shortage, proposed) with +/− steppers and a quantity input clamped to the line's shortage; `Confirm allocation` is disabled until the allocated total equals the stocked quantity — `POST /api/allocations` carries that stocked quantity as `allocation_quantity` (never the PN's whole available stock) — a refusal (a stale available stock, a line beyond its shortage) keeps the dialog open with the server's reason and a re-read suggestion under a new `device_event_id`, a lost response freezes the exact intent for a retry under the same `device_event_id`, success is reported only after the server's confirmation (the completed Work Orders named), the inventory and PN state refresh from the server and the barcode input regains focus; `Leave in stock — allocate later` records no allocation. The Completed Work Orders page replaces the former not-available placeholder: debounced server-side search over WO Number / PN / Job Number, the Done range (the named presets last 30 / 90 days and this / last year resolved server-side on the site's current date, all history, or custom explicit dates) and the due-outcome filter as query parameters, the sortable WO Number / Done / Received / Due headers (server-side, Done descending by default, the cycle returning to it — the Done header itself alternating ascending ↔ descending), keyset `Show more` paging in the chosen order that keeps the effective Done range its first page resolved and is offered only while a further row exists, with the loaded / matching counts, `Search all history` from a bounded empty result, and each row opening the read-only Work Order Details with its `Done` date and allocated quantities; the New Work Order lookup of a completed number opens those read-only details instead of creating a duplicate. The Phase 10 section below records the detailed implementation state.
- Phase 10.5 — Scan Station Receive Quantity Gap Closure is implemented end to end (the Application command `app/application/intake.py` with its read model, `POST /api/scan-stations/{id}/receipts` and the widened PN resolution, and the real three-view `Receive Quantity` wizard in the Scan Station): a canonical PN with no active Work Order Demand and no active quantity is now RECEIVED at a production station — a PN seen for the first time included — with the internal blank-number Work Order created or reused, the WorkOrderDemand, the QuantityFlow, the `PLANNED` AssignedRoute snapshot and the immutable `RECEIVED` Movement committing as one transaction. No migration was needed: the `RECEIVED` Movement shape already admits a Scan Station identity and a reason.
- Phase 11 — Read Models and Monitoring Views is implemented for the **Production Board** (the Area Board, Tracking, Machines per-PN assigned breakdown, and expected-duration monitoring remain open): the backend read model `app/application/production_board.py` on `GET /api/production-board` derives the Department-wide board from the current-position projection and the immutable Movement history — every PN with active quantity in the Department's Areas (or stocked quantity with an open demand) with its distribution per Area / Machine / External activity, the derived holding state (`MACHINE` / `QUEUE` / `PROCESSING` / `DONE` / `STOCKED`), the fixed entry timestamp of each position (the effective position-bearing Movement's `occurred_at`, the oldest of a grouped position), the stocked and scrapped quantities from effective `STOCKED` / `SCRAPPED` history, the OPEN demand context in the canonical demand ordering (PROJECT_PROFILE §18 — the first open demand defining the row's Hot rank, due date and received date; a completed Work Order never supplies a row's metadata), and the Department totals, in exactly that canonical demand ordering (stocked quantity is no sorting tier) — and the real board view on it (`src/api/production-board.ts`, `views/production-board/ProductionBoardView.tsx`, `board-feed.ts`): the Department resolved server-side (a single active Department, or `?department=<id>` on the display's URL), periodic auto-refresh with one request in flight at a time, the last complete rows kept with the explicit `Feed stale — reconnecting` status when a refresh or the connection fails and an immediate refresh on reconnection, the loading / error-with-Retry / empty states under the always-present board header, and the approved presentation of GUI_DESIGN §5 unchanged (kiosk mode, height-aware pagination and proportional rotation, automatic display scaling, manual navigation, the derived dwell / countdown / Total Days values from the shared UI clock). The Phase 11 section below records the detailed implementation state and the deliberate boundaries.

## Implementation Principles

- Build in vertical slices; complete one workflow end to end before expanding.
- Never transfer quantity before the system has a real quantity-introduction workflow: Work Order Intake and production release precede all real transfer workflows.
- Preserve Presentation → Application → Domain → Infrastructure.
- Use mock data only until the relevant backend slice exists; mock behavior never leaks into production.
- Ambiguity always requires explicit confirmation before any write; unknown or invalid input is rejected with no write.
- Production writes are blocked while disconnected; offline scan synchronization is deferred and unapproved.
- Do not implement ERP integration during MVP.
- Every behavior described as `deliberately absent`, `temporary`, `placeholder`, `later`, or otherwise deferred must name exactly one owning later phase, or be listed explicitly under `Deferred` / a canonical open decision. A phase must never close with an unowned target behavior.
- Before closing any phase, audit `PROJECT_PROFILE.md` + `GUI_DESIGN.md` + the current production code against this roadmap. Close the phase only when every in-scope target behavior is implemented and every intentional omission has an explicit later owner.

## Phase 1 — Repository Foundation

Scope:

- React + TypeScript frontend
- FastAPI backend
- PostgreSQL
- Alembic
- Docker Compose
- health endpoint
- frontend/backend/database connectivity
- formatter, linter, type checks, and test foundations

Completion criteria:

- Development environment starts from documented commands.
- Frontend calls the backend health endpoint; backend connects to PostgreSQL.
- Formatting, linting, type checks, and initial tests run successfully.

## Phase 2 — Frontend Design System and Application Shell

Scope:

- shared tokens
- dark and light contexts
- application routing/navigation
- approved mock views, including the Work Orders view in the application shell (New Work Order modal with optional WO Number/due dates, manual-first Add Part flow, OPEN Work Order line editing, calendar date inputs, mock validation, unsaved-change protection)
- fast connectivity detection: browser online/offline events plus ~1 s `/api/health` polling with focus/visibility recheck (WebSocket/SSE remain out of scope and deferred)
- development-only mock data behind a real production build boundary (mock-sentinel verification in the build)
- loading, empty, error, connectivity-loss, and long-data states

Phase 2 is frontend presentation plus development-only mock behavior.

Non-goals:

- production business rules in mock components
- domain implementation or database migrations
- backend business APIs
- persisted production writes (every Phase 2 save changes development-only mock state)
- ERP integration

## Phase 3 — Minimum Canonical Domain and Data Foundation

Only the foundation required by manual Work Order Intake and production release:

- Department
- Area
- Operation
- PartNumber master as optional current metadata keyed by the canonical PN string (uppercase, whitespace-free normalization; create-on-first-use — no preloaded catalog requirement; production tables keep their own canonical PN value and never reference the master by FK)
- WorkOrder (nullable external `work_order_number`; uniqueness for non-null numbers only)
- WorkOrderDemand (`request_type IN ('NEW','MODIFY')`)
- RouteTemplate
- RouteStep
- AssignedRoute (optional — only `PLANNED` flows carry one)
- QuantityFlow with `route_mode` (`FLOATING` default / `PLANNED`) and nullable `assigned_route_id` — **the data foundation must support Floating Routes from the start**
- PartMovement (including the typed `movement_reason` column concept for later Repair movements)
- derived current-position projection

Rules:

- Preserve canonical PartMovement field names when those fields are introduced: `station_id`, `occurred_at`, `server_received_at`, `device_event_id`, `movement_reason`.
- Do not introduce competing names such as `client_event_id`.
- Movement history is immutable; quantity integrity is enforced; domain invariants are covered by tests.
- Floating Route traces are derived from Movement history — no second mutable route history table.

## Phase 3.5 — Minimum Environment Setup (named prerequisite — no later phases renumbered)

The smallest administration capability required to operate the initial
production slices, deliberately separated from the later full Administration
phase (Phase 13). It sits after the domain foundation (Phase 3) and **before
any real production workflow needs a configured environment**: Phase 4 needs
configured Departments, Areas and Operations for release, and Phase 5 needs
active, Area-bound Scan Stations.

Scope — configuration management for exactly:

- Departments
- Areas (identity, color/display properties, terminal flag)
- Operations (per Area)
- Scan Stations (Station ID, bound Area, active status)
- Machines (per Area, lifecycle and maintenance status) — configured through
  **Management → Machines** (GUI_DESIGN §12), permission-based for authorized
  production roles rather than an Administration panel, but still part of this
  minimum setup prerequisite: Machines must be configured before real
  production runs
- Machine lifecycle event history — the dedicated append-only
  **`machine_lifecycle_events`** table (canonical name) persisting the
  `RETIRED` and `REACTIVATED` lifecycle events PROJECT_PROFILE §8.6 requires,
  created in this phase together with `machines`. A retirement or
  reactivation and its lifecycle event commit atomically — one transaction,
  no lifecycle change without its event and no event without its change —
  and recorded events are immutable/append-only. Each event records the
  canonical lifecycle context: event type, Machine identity, occurred time,
  reason, before/after state, and the previous and current Area when the
  Machine moved while retired. This is Machine lifecycle/history
  persistence, deliberately **not** a generic audit framework and **not**
  the Slice 1 `audit_events` mechanism: `audit_events` stays owned by
  Phase 4 and scoped to WorkOrder, WorkOrderDemand, and PartNumber
  (SLICE1_DATA_MODEL §16) — Machine never becomes an `audit_events` entity
  type, and Phase 3.5 creates no `audit_events` table. Actor identity on
  lifecycle events stays a nullable, reference-free value in Phase 3.5 — no
  Worker or User foreign key. Machine retire/reactivate is a Management
  action, so any future authenticated actor linkage belongs to Users and
  authentication (Phase 14); Workers are Scan-Station-scoped production
  audit identity, separate from Users, and are never associated with
  Machine lifecycle events.
- the required active/inactive flags and the exact barcode ownership
  PROJECT_PROFILE §10 defines — no other barcode namespace is invented:
  Areas carry `PF:AREA:<stable-id>`; Machines carry
  `PF:MACHINE:<asset-tag>`, always derived from the immutable Asset Tag;
  Departments have no barcode; Operations get no barcode field; Scan
  Stations are identified by their stable Station ID and Area binding —
  there is no `PF:STATION` or any other station barcode. This includes the
  Administration → Barcode configuration Asset Tag format (prefix +
  zero-padded numeric sequence, PROJECT_PROFILE §8.6) that Machine creation
  requires in order to auto-assign Asset Tags

Explicitly NOT in this prerequisite: no production workflow and no
production write of any kind — no Work Order intake or production release,
no QuantityFlow creation, no PartMovement workflow or movement-type
widening, and no transfer or Machine-assignment workflows (Phases 4–6 own
those); not the generic `audit_events` table (Phase 4, SLICE1_DATA_MODEL
§16); not the real Planned Routes management and not the real Part Numbers
management (both Phase 13, through Management → Planned Routes and
Management → Part Numbers); and none of the remaining administration
capability, which stays in Phase 13 and Phase 14: Workers, Users and roles,
authorization management, Worker-session policies beyond the immediate
workflow requirements, correction permissions, retention/archive/purge
policies, general settings, and every other nonessential administration
capability. The Phase 2 Administration view remains a development-only
visual shell/reference until this prerequisite and later Phase 13 make its
sections real.

## Phase 4 — Manual Work Order Intake and Production Release

The first business vertical slice, presented in the UI as the Work Orders view (GUI_DESIGN §11). It must:

- create/find WorkOrder,
- save a confirmed blank external WO Number as `NULL` on an internal Work Order (rendered `—`, never persisted as a placeholder; replaceable later through an audited edit — PROJECT_PROFILE §7 Work Order; no temporary number is generated),
- accept null WorkOrder and WorkOrderDemand due dates as valid data (PROJECT_PROFILE §8.2/§8.3),
- create/find the PartNumber master for the canonical PN (uppercase, whitespace-free normalization; create-on-first-use),
- provide the minimal operational capability to view/print the derived `PF:PN:<canonical-part-number>` barcode label (PROJECT_PROFILE §10) for a PartNumber created/found during intake — Phase 5 production scanning requires physical PN barcodes. This is the only Part Numbers capability in this phase: it does not make the full Management → Part Numbers screen real (Phase 13). In the UI the label is reached by activating the demand-line PN itself (GUI_DESIGN §11.2) — a **provisional** entry point, since no PartNumber metadata dialog can exist before Phase 13,
- create/update WorkOrderDemand,
- save demand separately from production quantity,
- explicitly release production,
- create QuantityFlow with its route mode (`FLOATING` default),
- snapshot an independent Route **only** for a `PLANNED` release — a Floating release has no AssignedRoute,
- append `RECEIVED`,
- establish current position,
- remain transactional and idempotent,
- not automatically merge with existing active quantity,
- enforce the Work Order Demand removal rule (PROJECT_PROFILE §13) transactionally in the Application/Domain layer: a WorkOrderDemand line may be deleted only while no production quantity has been released for it; once released, deletion must be refused, and removal must never cascade to the PartNumber master, QuantityFlows, PartMovements, release history, or other WorkOrderDemand records for the same PN.

Manual entry comes before file import. Development seed data may support UI development and tests, but seeded `RECEIVED` fixtures are not the product intake workflow.

## Phase 5 — Scan Station Transfer to an Area Queue

Pilot example: `Material -> Lathe queue`, where Lathe is configured as `Queue -> select Machine by scan`.

The slice must include:

- stable Scan Station configuration bound to Lathe,
- PN barcode resolution,
- source QuantityFlow resolution,
- Operation resolution,
- route validation,
- quantity validation,
- explicit ambiguity confirmation,
- immutable `TRANSFERRED` Movement,
- transactional current-position projection update,
- idempotent retry handling,
- keyboard focus restoration,
- recent scans and Area inventory refresh.

Candidate resolution must respect current position, route, Operation, station context, and valid deviations — never "every active flow outside the target Area".

Temporary limitation (lifted by Phase 8): full-QuantityFlow movement only. Partial movement was not to be claimed as supported before SPLIT (Phase 8); partial input was refused clearly with no write. The target GUI requirement for partial quantity (GUI_DESIGN §4.7) is fulfilled since Phase 8. Second temporary limitation: until Area completion exists (`AREA_COMPLETED` — Phase 6 for Machine Areas, Phase 7 for direct processing), Phase 5 transfers record `TRANSFERRED` alone; from the phase that introduces Area completion onward, a transfer from actively processing quantity appends `AREA_COMPLETED` + `TRANSFERRED` as one atomic application command (PROJECT_PROFILE §8.11).

Implementation state: this phase is implemented end to end (see Current State) — migration `0006_phase5_transfer`, the transfer command and the Scan Station read models under `/api/scan-stations/{station_id}/…` and `/api/areas/{area_id}/inventory`, and the real Scan Station frontend on them (keyboard focus restoration, the Last Action block, and the server-side inventory refresh are part of that wiring). Route deviation — Area or Operation — is confirmed explicitly with a reason and recorded on the `TRANSFERRED` Movement itself; the separate deviation/route-adjustment events and route editing belong to route editing (Phase 9+).

## Phase 6 — One-Shot Machine Assignment and Area Completion

- Machine barcode resolution (one-shot shortcut only — **no Machine session of any kind**)
- Machine/Area validation
- Machine-first (Machine scan opens the one-shot assignment dialog with the Machine preselected) and PN-first (assign action on queued quantity) entry points
- `ASSIGNED_TO_MACHINE`
- `RELEASED_FROM_MACHINE` (the `QUEUE` return action — returns unfinished or paused quantity to the queue, never completed work)
- **`AREA_COMPLETED` for Machine Areas** (PROJECT_PROFILE §7 Area Completion, §8.11, §12): the manual DONE action on Machine-assigned quantity (`DONE` and `QUEUE` stay two distinct actions), deriving `READY_TO_TRANSFER`, clearing `current_machine_id`, keeping the Area as location; a transfer from `ON_MACHINE` quantity appends `AREA_COMPLETED` + `TRANSFERRED` in one transaction; DONE is quantity-scoped and never a PN status, never `STOCKED`
- inactive Machine rejection
- one Machine may hold quantities of multiple PNs; one Machine behaves exactly like several (never auto-assign by Machine count)

Implementation state (complete — persistence, the Application commands, the read models and the real Scan Station frontend; audited against PROJECT_PROFILE §8.6/§8.11/§12/§15 and GUI_DESIGN §4 before closure. Area and scrap barcodes, Worker identity and badge gates, direct-processing DONE, Repair, Scrap and Undo stay honest placeholders in the real Scan Station view): migration `0007_phase6_machine_assignment` adds `quantity_flows.current_machine_id` (nullable FK, indexed), the Movement Machine references `part_movements.source_machine_id` / `destination_machine_id`, and `part_movements.command_sequence` — one `device_event_id` now identifies one **application command**, which may append several Movements (`UNIQUE (device_event_id, command_sequence)` replaces `UNIQUE (device_event_id)`; every earlier row is a one-Movement command with sequence 1), so `WHERE device_event_id = …` yields the complete command Undo (Phase 9) reverses as one; the movement-type check admits `ASSIGNED_TO_MACHINE`, `RELEASED_FROM_MACHINE` and `AREA_COMPLETED`, and the per-type shape check pins each: the three in-Area types stay in ONE Area at a Scan Station and carry exactly their Machine reference (assignment a destination Machine, release and completion a source Machine — Phase 7 widens completion to a Machine-less direct-processing branch), while `RECEIVED`/`TRANSFERRED` never reference a Machine. The holding state is **derived from the flow's latest Movement** (`ProcessingState`: `ASSIGNED_TO_MACHINE` → `ON_MACHINE`, `AREA_COMPLETED` → `READY_TO_TRANSFER`, everything else → `QUEUED`) — a NULL `current_machine_id` is never "queued" by itself — and the projection replay rebuilds Area, Machine and state from history. Application commands (`app/application/machine_processing.py`, endpoints `POST /api/scan-stations/{id}/machine-assignments` | `machine-releases` | `area-completions`, each 201 fresh / 200 idempotent replay / 409 mismatched reuse, also across command kinds): **assign** (QUEUED only; the Machine row is locked `FOR UPDATE` and re-read under the lock — retired, other-Area and maintenance Machines refused; several PNs may share one Machine; nothing is ever auto-assigned), **QUEUE** (`RELEASED_FROM_MACHINE`, ON_MACHINE only, derives QUEUED — never completed), **DONE** (`AREA_COMPLETED`, ON_MACHINE only, clears the Machine, keeps the Area, derives READY_TO_TRANSFER; allowed under a Maintenance override). A transfer of ON_MACHINE quantity appends `AREA_COMPLETED` (sequence 1, the source Machine recorded) then `TRANSFERRED` (sequence 2) as one atomic command under one `device_event_id`, replayed as a whole (the response carries `completed_movement_id` / `completed_machine_id`); queued or finished quantity transfers with `TRANSFERRED` alone. Every command is one transaction, serialized on the flow row lock (lock order flow → station → Machine → target Area → Operation), whole-QuantityFlow only as this phase shipped (partial assignment/QUEUE/DONE/transfer refused with zero writes until SPLIT arrived with Phase 8 — since lifted), and records the Operation carried forward from the flow's latest Movement plus the Scan Station. The Machine operational state stays derived (Maintenance override > assigned ACTIVE quantity = Running > Idle; exposed as `operational_state` / `assigned_quantity` on `/api/machines`) and `state_changed_at` moves only when that derived value actually changes; Machine retirement now locks the Machine row first and is refused while ACTIVE quantity is assigned (no flow lock taken, so no lock order contradicts the production commands). Read models (second backend round): `POST /api/scan-stations/{id}/machine-scans/resolve` resolves a `PF:MACHINE:<asset-tag>` barcode (or a manual Asset Tag) at the station into the **one-shot assignment context** — the Machine preselected (unknown 404; retired, other-Area or maintenance Machine 409, nothing resolved) plus every QUEUED flow of the station's Area to select or scan a PN for, several returned unpicked with `requires_selection`; nothing is stored server-side (no Machine session, no sticky state — the assignment command re-validates the Machine under its row lock, so a Machine retired or put under maintenance after the scan refuses the assignment with zero writes). PN-first: every flow in the read models carries `available_actions` by derived state (QUEUED → ASSIGN, TRANSFER; ON_MACHINE → DONE, QUEUE, TRANSFER — the transfer completing implicitly; READY_TO_TRANSFER → TRANSFER), the PN resolution flags `requires_selection` when several flows of the PN are in the Area (or several transfer candidates exist), and a Machine barcode on the PN scan is named as such and refused. `GET /api/areas/{id}/inventory` keeps the Phase 5 per-PN `lines` and adds the state split: `queued` / `queued_quantity` and `finished` / `finished_quantity` (Area summary — finished quantity never sits on a Machine card), and `machines` — one card per active Machine of the Area (retired excluded, with or without quantity) holding ONLY its ON_MACHINE quantity per PN, with the derived `operational_state`, `state_changed_at` and maintenance context; card quantity and state derive from the same flows, and `/api/machines` (`operational_state`, `assigned_quantity`) reconciles with them. Frontend (third round — GUI_DESIGN §4.11 implementation boundary, Phase 6): the real Scan Station renders the inventory split (Area queue, Machine cards holding only `ON_MACHINE` quantity with the derived state, `Finished — ready to move`), resolves Machine barcodes on the server into `Assign to Machine` with the Machine preselected (refused Machines are actionable notices, nothing resolved; the dialog's own scan input selects a queued PN or another Machine without discarding existing choices), offers PN-first `Assign to Machine` per queued quantity and `Complete Area processing on {Machine}` per Machine flow, carries the two distinct Machine-card actions DONE / QUEUE through quantity → summary → final question, announces the implicit completion on a transfer of `ON_MACHINE` quantity and reports both recorded events, and reuses the Phase 5 unknown-outcome/retry model (`src/views/scan-station/scan-station-write.ts`, `scan-station-machine-dialogs.tsx`, `api/scan-station.ts`); Management → Machines derives Running and the assigned total from `/api/machines`. Still absent as Phase 6 shipped: direct Area processing (arrived with Phase 7), SPLIT/MERGED (arrived with Phase 8), Worker identity and badge gates, Undo, Repair, Scrap, Stockroom.

## Phase 7 — Direct Area Processing (Areas Without Machines)

- Area without Machines takes direct processing ownership (no queue)
- Operation recorded and Machine null
- multiple-Operation confirmation
- **`AREA_COMPLETED` for direct processing**: the same manual DONE workflow without a Machine field, and implicit completion when directly processing quantity transfers to another Area (one atomic `AREA_COMPLETED` + `TRANSFERRED` command)
- exactly two Area modes exist: no Machines → direct processing; Machines → `QUEUE_AND_ASSIGN` (PROJECT_PROFILE §12) — no per-Area assignment-mode configuration and no single-Machine auto-assignment

Implementation state (complete — persistence, the Application command, the transfer widening, the read models and the real Scan Station frontend; audited before closure against this section, PROJECT_PROFILE §8.11/§12, GUI_DESIGN §4 and the Phase 5–6 command contracts — the derived two-mode rule with no per-Area configuration, Machine NULL and the explicit Operation on direct arrivals, exactly one `AREA_COMPLETED` per manual DONE, the atomic implicit completion versus `TRANSFERRED` alone from finished quantity, history/replay agreement, whole-command replay versus cross-intent conflict, one serial winner per flow, zero writes on invalid or stale input, the Phase 6 Machine-Area workflows unchanged, no mock state in the production Scan Station and no SPLIT/MERGED or later-phase behavior — with the full backend and frontend gates and the migration chain `head → 0007 → head → base → head` + `alembic check` verified green): migration `0008_phase7_direct_processing` widens only the `AREA_COMPLETED` branch of `ck_part_movements_movement_shape` — a completion carries its source Machine (Machine Area) or none (direct processing) and never a destination Machine; every other branch, the movement-type check, `command_sequence` and the idempotency key are untouched, no column or table is added (the holding state is derived, never stored; no `machine_assignment_mode`; `areas.worker_identification_mode` stays with Worker identification), and the downgrade refuses to drop a Machine-less completion. **The Area mode is derived, never configured** (`app.application.machines.areas_with_machines` / `area_has_machines`: at least one active, non-retired Machine → `QUEUE_AND_ASSIGN`, otherwise direct processing), and it is part of the state derivation (`projections.processing_state_of(movement_type, direct_processing=…)`): `ASSIGNED_TO_MACHINE` → ON_MACHINE, `AREA_COMPLETED` → READY_TO_TRANSFER, every other latest Movement → QUEUED in a Machine Area or **PROCESSING** in an Area without Machines. Consequently a production release into, or a transfer into, an Area without Machines hands the quantity straight to processing — no queue, `current_machine_id` NULL, the Operation recorded on the `RECEIVED` / `TRANSFERRED` exactly as Phases 4–5 resolve it (the single active Operation or the route-step Operation, else the explicit `operation_id`; several without a choice are refused with nothing written) — and adding the first Machine to an Area or retiring its last one moves the Area, and the quantity it holds, between the two modes without touching history (the retirement-versus-assignment regression test records this). Commands: the direct DONE (`app/application/direct_processing.py::complete_direct_processing`, reached through `POST /api/scan-stations/{id}/area-completions` with `machine_id` omitted — the same endpoint, request shape `AreaCompletionRequest`, response `machine_id` null) follows the Phase 6 protocol verbatim (shape → fingerprint with Machine None → idempotency → flow lock → re-check → station lock bound to the flow's Area → whole quantity → state), accepts PROCESSING only (finished quantity 409 "already completed"; quantity in an Area with Machines 409 "has Machines" — never completed without naming the Machine), and appends exactly one `AREA_COMPLETED` (`from_area_id = to_area_id`, Station recorded, Operation carried forward from the latest Movement, `source_machine_id` NULL, `command: {kind: DONE, size: 1}`), keeping the Area and deriving READY_TO_TRANSFER; the Machine-Area commands refuse directly processing quantity explicitly (DONE/QUEUE with a Machine 409 "not on a Machine", ASSIGN 409 "nothing to assign"). The transfer command judges the source mode under the flow lock: PROCESSING quantity, like ON_MACHINE quantity, is completed implicitly — `AREA_COMPLETED` (sequence 1, no Machine, no Machine lock) then `TRANSFERRED` (sequence 2) under one `device_event_id`, replayed as a whole, all-or-nothing at the database (`completed_movement_id` set, `completed_machine_id` null); READY_TO_TRANSFER quantity transfers with `TRANSFERRED` alone. Idempotency is unchanged (SLICE1 §14): a direct DONE id reused for a Machine DONE or vice versa is a 409 conflict, a race lost at COMMIT replays the winner; one transaction per command; lock order flow → station → (source Machine) → target Area → Operation; whole QuantityFlow only as this phase shipped — partial DONE/transfer refused with zero writes until SPLIT arrived with Phase 8 (since lifted). Read models: `GET /api/scan-stations/{id}/context.has_machines` and `GET /api/areas/{id}/inventory.has_machines` name the mode; every flow carries `processing_state` (`PROCESSING` added) and `available_actions` (PROCESSING → DONE, TRANSFER — never ASSIGN or QUEUE); the inventory adds `processing` / `processing_quantity` beside the Phase 6 `queued` / `machines` / `finished` split (an Area without Machines: no cards, queued and on-Machine structurally 0; a Machine Area: processing structurally 0); the projection replay (`rebuild_current_positions`) rebuilds Area, Machine and state — including PROCESSING → READY_TO_TRANSFER → transferred — from history plus the Areas' current Machines. Tests: `test_phase7_schema.py` (head-level coverage; `test_phase6_schema.py` pinned to `0007`), `test_direct_processing_api.py` (arrival by release and transfer, explicit Operation choice, the direct DONE and every refusal with zero writes, cross-intent idempotency, race lost at COMMIT, implicit completion as one command and its database-level atomicity, DONE versus transfer and two DONEs of one flow, projection replay, the mode following the Machines, and the Machine-Area regressions), and the Phase 5/6 suites updated only where a Machine-less Area now completes implicitly. The read models also expose on every flow the Operation RECORDED on its latest Movement as metadata (`operation`: id, code, name, `is_external`, `is_active`) — loaded whatever its current activation, so existing quantity keeps its recorded Operation after a deactivation, independent of the active Operations the station offers for new arrivals — and the station names the Operation and tells external processing apart from that metadata alone. Frontend (GUI_DESIGN §4.11 implementation boundary, Phase 7): `api/scan-station.ts` carries `PROCESSING`, the flow's recorded `operation`, the inventory `hasMachines` / `processing` split and a nullable `machineId` on the DONE command (omitted from the request for the direct completion); the real Scan Station takes the Area mode from the **freshest** server read — the rendered inventory's `has_machines` (re-read after every action, consistent with the flow states it carries) wins over the station context loaded earlier, and the PN action dialog derives its choices from the resolved flows' states alone, so a first Machine added or the last one retired after the page loaded never leaves the presentation or the actions on a stale mode — presents directly processing quantity as the `In processing` group (`External processing` when the flow's recorded Operation is external), carries the single `DONE` row action on exactly the `PROCESSING` rows reported with a `DONE` action (never `QUEUE`, never on finished rows, never in a Machine Area, disabled while writes are blocked), offers `Complete Area processing` per directly processing flow in the PN action dialog, reuses the Phase 6 Area Completion wizard with `machine: null` (no Machine field, the Operation row, the final question, the one-shot write model with its unknown-outcome retry and rejection handling), reports the direct completion after the server confirmed it and re-reads the Area, and names the implicit completion of directly processing quantity on transfer (`Recorded events: AREA_COMPLETED, then TRANSFERRED (one command)`, both events in the success notice) while finished quantity transfers with `TRANSFERRED` alone. Tests: `scan-station-direct.test.tsx` (direct inventory presentation, row and PN-first DONE, the wizard without a Machine, refusal and lost response, transfers out of and into a direct-processing Area, the disconnected guard, the context/inventory mode mismatch in both directions — a Machine added or retired after load, also while the station is open — and an inactive recorded Operation still presented as recorded) and the Phase 5/6 station suites adjusted to the Phase 7 semantics of a Machine-less Area.

## Phase 8 — Quantity SPLIT and MERGED Workflows

- partial movement
- preserved lineage
- quantity conservation
- `SPLIT` and `MERGED` Movement history
- quantity keypad supports valid partial amounts
- **SPLIT is the prerequisite for partial Repair** (Phase 9): before SPLIT exists, partial Repair is refused clearly with no write — it must not be claimed as supported

Implementation state (complete — persistence, the lineage derivation, partial quantity on every existing command, the explicit merge, the read models, the projection replay and the real Scan Station frontend; audited before closure against this section, PROJECT_PROFILE §8.7/§11/§12/§15, GUI_DESIGN §4 (§4.7 item 3, §4.8, §4.11) and the Phase 5–7 command contracts — quantity conservation and source closure on every split, the remainder keeping the source's state and route context, the untouched WorkOrderDemand, the no-split full-quantity regression, the server-judged `combine_groups` with the under-lock revalidation and the shared-provenance-only `source_route_template_id`, the whole-command idempotent replay from immutable state, the lineage-complete projection rebuild, and the documentation cleanup lifting the Phase 5–7 whole-QuantityFlow wording; with the full backend gate, the migration chain `head → 0008 → head → base → head` + `alembic check`, and the full frontend gate (`npm run check`: format, lint, typecheck, tests, build, production-boundary sentinel) verified green): migration `0009_phase8_split_merge` adds the `SPLIT` / `MERGED` movement types with ONE shared shape branch (`from_area_id = to_area_id`, Station recorded, no Machine reference — a lineage event never re-states a position), the Quantity Flow lifecycle checks (`status IN (ACTIVE, SPLIT, MERGED)`, `(status = ACTIVE) = (closed_at IS NULL)`), and the append-only `quantity_flow_lineage` edge table (`relation`, `parent_flow_id`, `child_flow_id`, the command's `device_event_id`; UNIQUE per (parent, child), parent ≠ child, raise-on-write trigger) — the same edge shape reconstructs a 1 → N split and an N → 1 merge, so the canonical illustrative `parent_flow_id` of PROJECT_PROFILE §8.7 is realized as edges, not as a column (SLICE1_DATA_MODEL §9/§18 updated accordingly); the downgrade refuses to drop lineage history. **Derivation** (`app/application/projections.py`): a flow's position-bearing Movement is its newest non-lineage Movement, or — for a flow whose only Movement is the lineage event that created it — the one it inherits from its parent (the parent's newest non-lineage Movement before the child existed, recursively; a merge validated that every source held the same position, so the lowest parent id is an equivalent deterministic choice); every read model and command derives state, Machine and Operation from that effective Movement, the Area from the flow's newest Movement, and the projection replay rebuilds every ACTIVE flow while consumed flows (parents in the graph) are closed and never active inventory. **SPLIT** (`app/application/lineage.py::stage_split`, used by every command through `machine_processing.split_if_partial`): a confirmed quantity strictly inside the flow's closes the source (`status = SPLIT`, `closed_at`, Machine projection cleared), creates two children with the source's PN, Area, Machine and route mode — a PLANNED source gives each child its OWN AssignedRoute snapshot copy (the one-to-one `assigned_route_id` constraint is preserved; two active flows never share a snapshot) positioned at the source's last known step — appends three `SPLIT` Movements (source consumption, selected child, remainder child; Σ children = source) and two lineage edges, and the action then applies to the selected child only (sequence 4 onward, the transfer's route references re-expressed in the child's snapshot copy), the remainder keeping exactly the source's state; the whole quantity never splits (no `SPLIT` when the action uses all of it) and `WorkOrderDemand` is untouched. Partial quantity is thereby supported on Assign, QUEUE, Machine DONE, direct DONE and Transfer from every source state (a partial transfer of ON_MACHINE / PROCESSING quantity completes and moves only the selected part; the remainder stays on the Machine / in processing), each as ONE atomic application command: same `device_event_id`, deterministic `command_sequence`, the same fingerprint on every row, replayed as a whole on retry (the response names `source_quantity_flow_id`, `remainder_quantity_flow_id`, `remainder_quantity`), a different payload under the same id refused, and two commands on one source serialized on the source row lock — the loser re-reads the closed source and is refused ("was split into separate quantities"), never consuming the quantity twice. **MERGED** (`app/application/merges.py`, `POST /api/scan-stations/{id}/merges`): an explicit command naming at least two distinct ACTIVE flows of one PN in the station's Area (locked ascending by id, then the station, then the shared Machine); compatibility is strict and judged under the locks — same derived processing state, same Machine, same recorded Operation, same route context (all FLOATING, or all PLANNED with structurally equal snapshots at the same last-known step) — any difference refused explicitly with zero writes and nothing ever merged automatically; the merge closes every source (`status = MERGED`), appends one `MERGED` per source (sequence 1..N) and one on the resulting flow (N + 1, the summed quantity, the shared position, a PLANNED result's own snapshot copy), and records one lineage edge per source. **Read models**: consumed flows never appear in the PN resolution, the Machine-scan context or the Area inventory; children and merge results carry the state, actions and Operation derived by lineage, and the Work Order context of the released flow(s) they descend from (none when a merge joins different demands — never guessed); the PN resolution adds `combine_groups` — the groups of at least two in-Area flows whose `merges.merge_context` (state, Machine, Operation, route context) is identical, computed by the same helper the command enforces, so the frontend carries no compatibility rule of its own. **Frontend** (GUI_DESIGN §4.7 item 3, §4.8 and the §4.11 Phase 8 boundary): `api/scan-station.ts` carries `combineGroups`, the split fields of every command response and `combineQuantities()` (`POST …/merges`); every wizard — transfer, `Assign to Machine`, Machine `DONE` / `QUEUE`, direct `DONE` — validates 1..MAX in the view (`quantityValid`), freezes the confirmed quantity for the summary and the request, shows `{n} pcs of {MAX}` with a Remaining row and names the remainder in the final question, and the notice / Last Action report the server's split (`SPLIT · {m} pcs remain …`); the full quantity keeps the whole-flow workflow unchanged. `scan-station-combine-dialog.tsx` is the `Combine quantities` workflow (portions as `{qty} pcs · {state} · {Operation}`, all preselected, at least two required, result preview, `Confirm combine`, `useOneShotWrite` with the Phase 6 refusal / unknown-outcome model), offered by the PN action dialog for exactly the server-reported groups; success is reported only after the server confirmed the `MERGED` command, then the Area is re-read and focus restored. GUI_DESIGN gained the Phase 8 boundary and post-v18 entry 19; mockup v18 demonstrates the workflow (a second `81-1042` portion at Deburr, the Combine quantities dialog) with a local stand-in for the server's judgement. Tests: `test_phase8_schema.py` (head-level; `test_phase7_schema.py` pinned to `0008`), `test_quantity_split_merge_api.py` (partial Assign / QUEUE / Machine DONE / direct DONE / Transfer from every state with conservation, lineage, the untouched demand and the Machine totals; the full-quantity regression; PLANNED snapshot copies, the child's recorded step and deviation, FLOATING children without snapshots; the merge with ancestry, on a Machine, of PLANNED flows, every incompatibility and station precondition; whole-command replay, conflicting reuse across quantities and kinds, a blinded retry serialized on the flow lock and one lost at COMMIT persisting nothing of its staged split; two partial commands on one source, stale commands on consumed flows; the projection replay across a lineage tree), and the Phase 5–7 suites adjusted where partial quantity now splits instead of being refused; frontend `scan-station-phase8.test.tsx` (partial Transfer / Assign / QUEUE / Machine DONE / direct DONE with the remainder before and after the write, the full-quantity regression, the lost-response retry of a partial action, Combine visibility for server-reported groups only, source selection, the confirmation, the recorded combine with the inventory and totals afterwards, the server refusal, the lost-response retry under the same `device_event_id`, focus restoration) and the Phase 5–7 station suites adjusted to the 1..MAX validation; the production-boundary test keeps the mock preview out of the production bundle.

## Phase 9 — Undo, Corrections, and Auditable Quantity Events

- `REVERSED` Movement; Undo reverses the complete application command (never one arbitrary row of a multi-Movement action — e.g. an implicit `AREA_COMPLETED` + `TRANSFERRED` transfer reverses as one), with a summary confirmation before applying
- original Movement preserved
- authorization
- reason when configured
- projection restoration
- audit visibility
- **Repair movements**: `TRANSFERRED · movement_reason REPAIR` with mandatory reason; full-flow Repair from this phase, partial Repair only on top of SPLIT (Phase 8)
- **Scrap**: canonical `SCRAPPED` events from the PF:SCRAP counting workflow — one auditable operation per confirmation; reconciliation `introduced = active + stocked + scrapped`
- **Quantity additions**: `QUANTITY_ADJUSTED · direction INCREASE` with mandatory reason, never altering requested quantity

Implementation state (complete — persistence, the reversal-aware derivation, the Undo command with its preview read model, Repair on the transfer command, Scrap, quantity additions, the Scan Station read-model widening, and the real Scan Station frontend for the four correction workflows; audited before closure against this section, PROJECT_PROFILE §8.11/§11/§14/§15/§16, GUI_DESIGN §3 and §4.5–§4.11, and the Phase 5–8 command and lineage contracts — the whole-command reversal (never one arbitrary Movement row) with the originals and the append-only lineage untouched, quantity and projection restoration for partial, multi-Movement and lineage commands verified against the history-only replay, the idempotent Undo retry with the fingerprint conflict on a mismatched id reuse and the database UNIQUE backstop against a concurrent double reversal, the explicit mandatory-reason Repair creating no demand with partial Repair riding the Phase 8 SPLIT, the one-auditable-operation Scrap and the exact `introduced = active + stocked + scrapped` reconciliation, the addition never touching a requested quantity, one serial winner per flow under the established lock order with zero writes on every refusal, `PF:SCRAP` rejected everywhere outside the Scrap workflow, the frontend reporting recorded only after the server confirmed with every lost-response retry keeping its `device_event_id`, the mock-free production Scan Station, and the deliberate absence of Phase 10+, Worker-session and Phase 13–14 authorization behavior; with the full backend gate, the migration chain `head → 0009 → head → base → head` + `alembic check`, and the full frontend gate (`npm run check`: format, lint, typecheck, tests, build, production-boundary sentinel) verified green): migration `0010_phase9_undo_corrections` widens the movement-type check with `SCRAPPED` / `QUANTITY_ADJUSTED` / `REVERSED` and the per-type shape check with their branches (a `SCRAPPED` stays in ONE Area at a Station and may name the source Machine the quantity left; a `QUANTITY_ADJUSTED` introduces quantity like a `RECEIVED` but always scan-driven; a `REVERSED` records the compensating motion at a Station — cross-Area for a reversed `TRANSFERRED` — and never re-states a Machine), adds `part_movements.movement_reason` (only `REPAIR`, only on a `TRANSFERRED`), the free-text `reason` (mandatory for Scrap, adjustments and every Repair — CHECK-enforced) and `reverses_movement_id` (FK, set exactly on a `REVERSED`, **UNIQUE** — the database itself refuses a second reversal of one Movement, including a concurrent-undo race), and widens `quantity_flows.status` with `SCRAPPED` / `REVERSED`. **Derivation** (`app/application/projections.py`): every derivation EXCLUDES the reversed pair — the `REVERSED` rows and the originals they reference — so the effective position-bearing Movement of a flow whose newest command was undone is the Movement before that command, restoring Area, Machine, Operation, route position (`lineage.last_known_step_id` and the transfer's route assessment skip reversed rows too) and holding state at once, also through lineage; lineage edges of an undone SPLIT/MERGED stay stored (append-only) but no longer count (`consumed_flow_ids`), the projection replay treats a flow with no effective Movement (creation undone) or a newest-effective `SCRAPPED` as inactive, and `visited_area_ids` is the effective lineage-aware visit history Repair validates against. **Undo** (`app/application/undo.py`; `GET /api/scan-stations/{id}/undo-preview/{device_event_id}`, `POST /api/scan-stations/{id}/undos`): reverses the COMPLETE committed command under the involved flow row locks (ascending), the station lock and the Machine/Area locks — one compensating `REVERSED` row per original in reverse order under the Undo's own `device_event_id`; flows the command closed reopen as ACTIVE with the projection restored from exactly the replay derivation, flows it created close as `REVERSED`, Machine assigned totals and `state_changed_at` are re-judged under the Machine locks, and quantity is conserved because a reversal never changes any flow's quantity. Eligible is only the most recent EFFECTIVE operation of every involved flow (a command followed only by reversals stays eligible — the §16 "context advances" rule), recorded at THIS station, not Management-recorded, not already reversed and not itself an Undo (a reversal is permanent and corrected forward); restoring onto a retired Machine or into a deactivated Area is refused (the Area is locked like a transfer destination). The preview serves the §16 summary confirmation (original action rows, quantity, source/destination, Machine, timestamp, the exact restored effect) with the honest eligibility verdict; the command re-judges everything under its locks, replays idempotently from the immutable `undo` metadata block, and refuses a mismatched id reuse. Authorization and the reason-when-configured rule stay OUT deliberately: no Users/RBAC and no such configuration exist before Phases 13/14, and nothing pretends otherwise. **Repair** (`app/application/transfers.py`): the explicit `repair` intent + mandatory `repair_reason` on the SAME transfer command records `movement_reason = REPAIR` and the reason on the `TRANSFERRED` row — no Repair aggregate, Request Type or demand; the destination must be previously visited per the effective history (else 409, zero writes), partial Repair rides the Phase 8 SPLIT inside the same command, a PLANNED deviation is still confirmed separately with its own reason, and the fingerprint covers the intent so a Repair and a plain transfer are different intents under one id; the PN resolution marks each candidate `repair_available`. **Scrap** (`app/application/quantity_events.py`; `POST /api/scan-stations/{id}/scraps`): one confirmed action appends ONE auditable `SCRAPPED` operation with its mandatory reason (PF:SCRAP counting stays client-side and writes nothing); the flow — or the split-off part, partial via the same in-command SPLIT — closes (`status = SCRAPPED`) and leaves active production with history, lineage and last position complete; ON_MACHINE quantity records and releases its Machine under the Machine lock while a partial scrap's remainder stays ON the Machine; the PN resolution reports `scrapped_quantity` (net of reversed scraps — the §11 reconciliation term). **Quantity addition** (`quantity_events.add_quantity`; `POST /api/scan-stations/{id}/quantity-additions`): found physical quantity enters as a NEW FLOATING QuantityFlow whose first Movement is `QUANTITY_ADJUSTED` with `direction INCREASE` in its metadata, the mandatory reason and the Station — introduced like a release (the reconciliation counts `RECEIVED` + `QUANTITY_ADJUSTED`), never hidden as a transfer, never touching any requested quantity, carrying no guessed demand context, entering the queue or direct processing by Area mode; refused when the PN has no ACTIVE quantity in the station's Area (that is the Receive Quantity intake) and the target Area is locked like a transfer destination. All Phase 5–8 contracts hold unchanged: one transaction per command, whole-command idempotency with fingerprint conflicts and races lost at COMMIT replaying the winner, the flow → station → Machine → Area → Operation lock order, zero writes on every refusal, and the projection rebuildable from Movement history alone (replay verified after every reversal). The `available_actions` read model adds `SCRAP` in every state. **Frontend** (GUI_DESIGN §4.11 implementation boundary, Phase 9): the real Scan Station wires all four workflows to this API through `src/api/scan-station.ts` and `views/scan-station/scan-station-correction-dialogs.tsx` — the PN action dialog offers `Add more quantity` (always, beside existing in-Area quantity), `Return quantity for repair` (only for `repair_available` candidates; the SAME transfer wizard with the explicit Repair intent, its own mandatory reason field and `TRANSFERRED · REPAIR intent` on the summary — a plain transfer to the same Area stays a separate choice) and one `Scrap damaged quantity` choice per in-Area flow; the Scrap dialog counts PF:SCRAP scans locally (−1/reset, nothing recorded while counting, the main scan input keeps rejecting the barcode), and the `⟲ UNDO` region of the Last Scanned PN block targets the most recent ELIGIBLE completed PN operation: the session's completed-command stack is walked newest-first through the server's `undo-preview` (the authority on eligibility, reads only — a newer ineligible command is skipped, with nothing eligible Undo disables with a notice naming the reason, the Last Scanned PN block shows the server-derived target rather than the raw newest session entry, and a nothing-reversible verdict revalidates on every later server sync), the first eligible preview shows the structured summary (original action, quantity, source/destination, Machine — never dropped: a Machine of another Area keeps the explicit `Machine #<id>` fallback —, timestamp, restored effect), the warning-toned final question follows (`Reverse this action?` / `Yes — reverse it`), the reversal submits under the Undo's OWN `device_event_id`, and on success the reversed entry leaves the stack and the walk re-resolves the target from the server, so the Last Scanned PN advances to the next eligible previous operation — or Undo disables at once with nothing eligible left; every write follows the shared one-shot model (success only after the server confirmed, frozen intent with `Retry the same …` on a lost response, refusals kept in the dialog with the Area re-read, then inventory refresh and barcode-input focus restoration). Tests: `test_phase9_schema.py` (head-level; `test_phase8_schema.py` pinned to `0009`), `test_undo_api.py` (every command kind reversed incl. the implicit-completion transfer, SPLIT-prefixed partials, merges, Scrap and additions; conservation, Machine totals, route-position restoration, consecutive undos walking back, every eligibility refusal with zero writes, the preview verdicts, replay/mismatch, and the threaded double-undo race with the DB UNIQUE backstop) and `test_phase9_corrections_api.py` (Repair full/partial/unvisited/reason rules/deviation interplay, Scrap full/partial/ON_MACHINE/refusals, additions with Area-mode state, the in-Area precondition and Operation ambiguity, and the `introduced = active + scrapped` reconciliation); frontend `scan-station-phase9.test.tsx` (Undo summary/final question/confirm with the stack advancing, the newest-first eligibility walk — a newer ineligible command opening the older eligible one, every command ineligible leaving no actionable Undo, the cross-Area reversal keeping the source Machine identity —, refused and lost-response Undo, Repair full/partial/not-offered with the plain transfer kept plus the Transfer-or-Repair choice and its eligible-only source subset for a PN with no in-Area quantity, Scrap counting/one-write/cancel/lost-response, the addition with no default and no MAX, offline write-blocking, PF:SCRAP rejection on the main input, refresh and focus restoration).

## Phase 10 — Stockroom and WorkOrderAllocation

- `STOCKED` Movement
- available stocked quantity
- suggested allocation follows the canonical demand ordering (PROJECT_PROFILE §18):
  1. highest manager-defined Work Order Demand priority
  2. within the same priority: dated demand earliest-first; undated demand after all dated demand, ordered by parent WorkOrder received_date (oldest first)
- equal values resolved by a stable deterministic tie-breaker (implementation detail, not a business rule)
- WorkOrderAllocation recorded separately from PartMovement
- the allocation adjustment capability (the auditable reversal) — its role-based authorization is enforced by Phase 14, never earlier

Implementation state (complete — backend: persistence, the `STOCKED` command, the allocation commands and read models, completion derivation and the completed history; frontend: the Stockroom station's receiving workflow with the allocation dialog of GUI_DESIGN §10 and the Completed Work Orders page of §11.5; audited before closure against this section, PROJECT_PROFILE §8.12 and §18, GUI_DESIGN §10 and §11.5, and the Phase 5–9 command, lineage and correction contracts — only the terminal Stockroom arrival records `STOCKED` while a transfer into a terminal Area stays refused under the locked Area re-read, partial stocking rides the Phase 8 SPLIT inside the one command, the stocked and available quantities stay derived and a `STOCKED` command is never an Undo target (the eligibility walk refuses it and every earlier command of that quantity is no longer its newest effective operation), one serial winner per flow and per PN under the established locks so no physical quantity is stocked or allocated twice, Allocation never referencing a Movement or a Quantity Flow, the canonical suggestion order, the shortage and available-stock caps, the append-only reversal as the only adjustment, completion derived from full allocation with the completed Work Order leaving the active list yet found by the exact `number=` resolution so its number is never duplicated, the frontend reporting stocked / allocated / completed only after the server's answer with every lost-response retry keeping its `device_event_id`, the mock-free production module graph, and the deliberate absence of Phase 11+, Worker-session and Phase 13–14 authorization behavior; the audit closed three findings — a demand save that had loaded its lines before a concurrent partial allocation committed judged the committed-quantity floor on the stale identity-map copy instead of the row re-read under its lock (now `populate_existing` on the demand lock, so `allocated_quantity > requested_quantity` is unreachable in either arrival order), a demand line whose allocation had been fully reversed failed at COMMIT on the allocation FK when removed (now refused up front — allocation history stays with the line), and the Completed Work Orders page could never show its "none ever" state because the default 90-day window always counted as a filter (the endpoint now returns the whole-history `history_total` beside the matching `total`) — plus the receiving allocation's Enter-to-confirm and the completed Work Orders named by number in the success notice; with the full backend gate, the migration chain `head → 0010 → head → base → head` + `alembic check`, and the full frontend gate (`npm run check`: format, lint, typecheck, tests, build, production-boundary sentinel) verified green): migration `0011_phase10_stock_allocation` — the movement-type check admits `STOCKED` with a shape branch identical to `TRANSFERRED` (from one Area into a DIFFERENT Area at a Station, no Machine; the terminal flag of the destination is an Application rule judged on the locked Area re-read), `quantity_flows.status` admits `STOCKED` (a closed, manufacturing-complete flow — `closed_at` agreement unchanged), `work_orders.completed_at` arrives with the partial index `(completed_at, id) WHERE completed_at IS NOT NULL`, and the append-only `work_order_allocations` table records one row per allocation event — canonical PN by value, `work_order_demand_id`, positive `quantity`, `source` (`STOCKROOM` / `MANAGEMENT`), `is_manual_override`, `allocation_reason` (CHECK-mandatory on a reversal), `reverses_allocation_id` (FK, set on a reversal, UNIQUE — one reversal per allocation even under a race), the recording `station_id` (NULL from Management), a reference-free `actor_reference`, `allocated_at`, `device_event_id` + `command_sequence` (UNIQUE) and the fingerprint/command metadata — with a raise-on-write trigger; the downgrade refuses to drop Phase 10 history. **`STOCKED`** (`app/application/stockroom.py` → `transfers.record_arrival(kind="STOCK")`, `POST /api/scan-stations/{id}/stockings`): one arrival command mechanism serves the transfer and the stocking — the explicit source flow with its Area precondition, the confirmed terminal destination checked under the station row lock, the lock order flow → station → source Machine → target Area → Operation, the Operation resolved from the terminal Area's configuration, the route assessment (a Planned Route whose last step is the Stockroom is ON_ROUTE and records the step; another terminal Area is a confirmed deviation), partial quantity through the in-command SPLIT (three `SPLIT` rows, then the action rows — the remainder stays at the source in its state), the implicit `AREA_COMPLETED` of ON_MACHINE / PROCESSING quantity (the Machine re-judged under its lock), whole-command idempotency (replay / mismatched reuse — a stocking and a transfer are different intents under one id / race lost at COMMIT), and zero writes on every refusal (a non-terminal destination, a transfer into a terminal Area, the Repair intent, a stale source, an exceeding quantity, an inactive Area or station). The stocked flow closes (`status = STOCKED`, `closed_at`, Machine cleared, the Stockroom as its last position): it is absent from every read model and from the projection replay (`projections.CLOSING_MOVEMENT_TYPES`), refuses every later production command, and is never undone (`undo._ineligibility` refuses a `STOCK` command explicitly — PROJECT_PROFILE §32 open decision 1, and an allocation may already depend on the quantity). The PN resolution reports `stocked_quantity`, `available_stocked_quantity` and, at a Stockroom station, `stock_available` for its candidates; the §11 reconciliation `introduced = active + stocked + scrapped` holds (`projections.stocked_quantity_of` is the stocked term). **WorkOrderAllocation** (`app/application/allocations.py`, `app/api/allocations.py`): every quantity is derived from history — the PN's stocked quantity from effective `STOCKED` Movements, its active allocation from allocation rows no reversal references, available = stocked − active; `work_order_demands.allocated_quantity` is a maintained projection of the demand's active rows (updated under the demand row lock, `rebuild_allocated_quantities` replays it). The suggestion (`suggest_allocation`, `GET /api/allocations/suggestion?part_number&quantity`) walks the PN's outstanding demand in the canonical order (`canonical_demand_order`: `priority_rank ASC NULLS LAST` — rank #1 first —, `due_date ASC NULLS LAST`, the parent Work Order's `received_date ASC` for UNDATED demand only — a `CASE` that is NULL for dated demand, so dated lines sharing a due date fall through to the tie-breaker —, demand id ASC) and proposes for each line `min(remaining shortage, still unallocated)` with the quantity capped at the available stocked quantity — every outstanding line is listed (proposed 0 included) with its requested, previously allocated, remaining shortage and proposed quantity, the surplus reported as `unallocated_quantity`. The confirmation (`confirm_allocation`, `POST /api/allocations` with `station_id` for the Stockroom receiving confirmation — the station must be active and bound to a terminal Area — or without it from Management) is ONE transaction: input shape (the explicit `allocation_quantity` — the just-stocked quantity the operator confirmed — with the lines required to sum to exactly it, PROJECT_PROFILE §18 "the total active allocation must equal the portion of stocked quantity being allocated") → fingerprint (covering the allocation quantity) → idempotency fast path → the per-PN advisory lock (`partflow:allocation:part-number:` namespace, serializing every allocation and reversal of one PN) → the demand row locks ascending → the Work Order row locks ascending → idempotency re-check → validation under the locks (PN agreement; a line never beyond its remaining shortage — allocation never exceeds requested quantity, no "authorized correction" exists before Phase 14; the allocation quantity never beyond the derived available stocked quantity — a figure that shrank since the dialog opened is a 409 with nothing written) → the rows with `is_manual_override` set where the confirmed quantity differs from the canonical suggestion for the confirmed total → the projections → COMMIT (or the replay of a race winner). Completion is derived (`_apply_completion`): a Work Order whose every demand line is fully allocated gets `completed_at = now()` from the completing allocation event (recorded in the rows' command metadata — `completed_work_order_ids` / `reopened_work_order_ids` — and replayed verbatim), reads as `COMPLETED`, leaves the active `GET /api/work-orders` list (the exact `number=` resolution still finds it, so a completed number can never be duplicated) and becomes read-only history: every PATCH, demand-line removal and release is refused with a 409 that writes nothing (the save locks the Work Order row after the demand locks and judges completion on the locked re-read, so a completing allocation and a save have one serial outcome; a release refuses a demand already fully allocated from stock under the demand lock). The reversal (`reverse_allocation`, `POST /api/allocations/{id}/reversals`) appends the REVERSAL row referencing the allocation with its mandatory reason — once per allocation (checked under the locks, the UNIQUE the backstop against a race), never a reversal of a reversal —, returns the quantity to the PN's available stock, lowers the demand projection, and clears `completed_at` when the Work Order was complete (`rebuild_completed_at` replays the done date as the newest effective allocation row of a fully allocated Work Order). `GET /api/allocations` lists allocation and reversal rows for audit display; `GET /api/work-orders/completed` is the history read model of GUI_DESIGN §11.5 (search over WO Number, PN and Job Number; the Done range as either a named `done_range` preset — `LAST_30_DAYS`, `LAST_90_DAYS`, `THIS_YEAR`, `LAST_YEAR`, resolved on the server against the site's current date in `SITE_TIMEZONE`, never against the browser's clock — or the Custom range's explicit inclusive `done_from` / `done_to` dates, never both; `due_outcome` ALL / ON_TIME / LATE / NO_DUE_DATE; `sort` DONE / RECEIVED / DUE / NUMBER with `direction` — Done descending by default, NULL values last in either direction, the id as the deterministic tie-breaker —; keyset paging on an opaque cursor bound to that order which also carries the done dates a preset resolved to at the first page, so every continuation of one loaded query keeps the same effective range even when the site's midnight or New Year passes between two pages — while a new first page resolves the preset on the new site date — and which is issued only while a further row exists, so a history ending exactly on a page boundary never offers an empty `Show more`; the matching `total` and the filter-independent `history_total` that tells "none ever" from "none in this range"); every date-versus-timestamp judgement of the history — the Done range bounds, the due outcome ON_TIME / LATE and each row's `done_date` / `due_outcome` / `days_late` — follows ONE rule, the completion instant in the site calendar (`SITE_TIMEZONE`, an IANA zone validated at startup, `UTC` by default), applied in SQL for the filter and in Python for the row so the two can never disagree. Movement history is never touched by any allocation command. Tests: `test_phase10_schema.py` (head-level; `test_phase9_schema.py` pinned to `0010`) and `test_stockroom_allocation_api.py` (the `STOCKED` arrival from direct processing and from a Machine, partial stocking through SPLIT, the Planned Route ending at the Stockroom, every refusal with zero writes, replay / mismatch / cross-kind reuse, the one-winner race on the flow lock, the not-undoable stocked command and the stale commands on a stocked flow, the reconciliation; the canonical ordering with all four criteria, the deterministic tie-breaker and the received date ordering undated demand only; the confirmation with completion, overrides, the explicit allocation quantity (missing, disagreeing, zero, negative), the stale available-stock precondition, every refusal, replay / mismatch, the paused two-station race stopped by the per-PN lock and the allocation-versus-in-flight-stocking interleaving; the reversal reopening a completed Work Order, once only, and the threaded double-reversal race; the read-only completed history with its refusals and the allocated-quantity floor, the history endpoint, immutable allocation rows, and the projection replays). **Frontend** (`frontend/src/api/allocations.ts`, `scan-station.ts` — `stockAtStationArea`, the resolution's `stocked_quantity` / `available_stocked_quantity` / `stock_available` —, `work-orders.ts` — `listCompletedWorkOrders`, `completed_at`; `views/scan-station/ScanStationView.tsx`, `scan-station-allocation-dialog.tsx`; `views/work-orders/CompletedWorkOrdersView.tsx`, `WorkOrdersView.tsx`, `WorkOrderDetailPanel.tsx`): the Stockroom station is the same Scan Station shell — a PN scan at a station bound to a terminal Area whose resolution reports `stock_available` opens `Receive into Stockroom` (one source: directly; several: the explicit source selection titled for stocking) instead of the ordinary transfer, and a terminal station with nothing to receive explains it (the stocked and not-yet-allocated quantities named) with nothing recorded; the receiving wizard is the transfer wizard carrying the stocking intent (quantity 1..MAX, the remainder shown before and after the write, the route deviation confirmation unchanged, `Recorded event(s)` naming `STOCKED`, `Confirm stocking` the only write point) on `POST /api/scan-stations/{id}/stockings` through the one-shot write model (frozen intent, the same `device_event_id` on the retry of a lost response, the replay reported as already recorded). After the server confirmed the `STOCKED` write — the history entry recorded, no Undo target (the stocked command is never undone) — the allocation dialog loads the server's suggestion for exactly the stocked quantity and renders the outstanding lines in the canonical order (rank badge, Work Order, requested, previously allocated, remaining shortage, `Allocate now` with +/− steppers and a numeric input clamped 0..shortage, the adjusted lines marked with the suggested figure), keeps the allocated total against the stocked quantity visible (`still to allocate` / `too many`, and the case where the outstanding shortage cannot absorb the whole quantity named), and enables `Confirm allocation` only when the total equals the stocked quantity; the confirmation sends that quantity as `allocation_quantity` with the lines and the station. A server refusal (the available stock shrank since the dialog opened, a line beyond its shortage) keeps the dialog open with the server's reason, re-reads the suggestion and re-seeds the lines under a NEW `device_event_id` (a different intent), while a lost response freezes the lines and offers `Retry the same allocation` under the SAME `device_event_id` — no second allocation is ever recorded; success is reported only after the server's answer (the completed Work Orders named), the inventory and the station context reload from the server and the barcode input regains focus. `Leave in stock — allocate later` closes the dialog with nothing allocated (the quantity stays available for a Management allocation), a PN with no outstanding demand is told so, and a write-blocked station (offline, backend unavailable) disables both confirmations. The Completed Work Orders page (`/management/work-orders/completed`) is a real view on `GET /api/work-orders/completed`: debounced search over WO Number / PN / Job Number, the Done range (the named presets last 30 days, last 90 days — the default —, this year and last year sent by name for the server to anchor to the site's date, all history, or custom from / to as explicit inclusive done dates), the due-outcome filter and the sort column / direction sent as query parameters, every row's done date and due outcome rendered as the server's verdict, the first 50 rows of the chosen order with `Show more` continuing on the server's opaque keyset cursor for that order and range, shown only while the server reports a further row and the summary keeping the loaded / matching counts, the `none ever` / `none in this range` empty states with `Search all history` from a bounded empty search, and the whole-row activation opening the Work Order Details read-only with `Done <date>` on the meta line and `Allocated n/m` per line; a completed Work Order leaves the active list (the server excludes it) and the New Work Order lookup of its number opens those read-only details instead of duplicating it. Frontend tests: `scan-station-phase10.test.tsx` (full and partial receiving through the server split, the source ambiguity, the suggestion rows in canonical order, the manual adjustment with `Confirm allocation` disabled while the total differs, the request carrying the stocked quantity as `allocation_quantity` — never the whole available stock —, the stale-stock refusal with the refreshed suggestion and no success, the refused adjustment retried under a new id, the lost allocation and stocking responses retried under the same id without a duplicate, the stocking refusal opening no allocation, leaving the quantity in stock, no outstanding demand, the offline write block), `completed-work-orders.test.tsx` (the history search, the Done range, the due-outcome filter, keyset paging, the read-only details, active → completed, no duplicate of a completed number), the updated scan-station suites for the terminal-Area resolution, and `production-boundary.test.ts` (the Completed Work Orders page is a real view importing no mock history). Deliberately absent: role-based authorization of the adjustment (Phase 14 enforces who may allocate and reverse; the capability itself is complete here — `actor_reference` stays NULL and `source` records where until an authenticated identity exists), Worker identity (Phase 13), the Management allocation and reversal screens beyond the API (they arrive with the Phase 14 authorization they depend on), any return of stocked quantity to production (PROJECT_PROFILE §32.1), and the Phase 11 monitoring read models.

## Phase 10.5 — Scan Station Receive Quantity Gap Closure (named corrective prerequisite — no later phases renumbered)

This corrective phase closes a workflow gap found after Phase 10: Phase 5 intentionally deferred Scan Station `Receive Quantity`, and Phase 9 still distinguishes it from `Add more quantity`, but no later phase owned its implementation. It must be completed before the remaining Phase 11 work is considered complete and before Phase 12 starts.

Scope:

- implement the real Scan Station **`Receive Quantity`** workflow of GUI_DESIGN §4.7 / PROJECT_PROFILE §14 when a canonical PN has no active Work Order Demand, including a PN seen for the first time;
- manual PN entry and `PF:PN:<part-number>` scanning resolve through the same canonical PN rule: trim surrounding whitespace, reject internal whitespace, canonicalize to uppercase, and create the optional PartNumber master on first valid use;
- keep the approved three-step workflow: settings → quantity → confirmation, with editable defaults `Request Type = MODIFY` and `Route Mode = FLOATING`, optional due date, Planned Route selection only for `PLANNED`, the station Area / resolved Operation context, reason/notes, and the canonical internal Work Order behavior; `Confirm receipt` is the only write point;
- create or reuse the internal WorkOrder / WorkOrderDemand exactly as PROJECT_PROFILE §14 defines. Several plausible blank-number MODIFY Work Orders require explicit selection — never a guessed first match;
- a confirmed receipt creates the production quantity transactionally: the required business-demand records, QuantityFlow, an independent AssignedRoute snapshot only for `PLANNED`, the immutable `RECEIVED` Movement, and the current-position projection commit atomically or not at all;
- a scan-driven `RECEIVED` records the Scan Station identity and resolved Operation. The station must still be active and bound to the confirmed active, non-terminal Area when the command commits; stale station / Area / Operation context is refused with zero writes;
- use the existing one-shot command/idempotency model: one `device_event_id` per frozen intent, deterministic fingerprint, same-intent replay, conflicting reuse rejected, lost-response retry under the same id, and success displayed only after server confirmation;
- refresh station context/inventory and restore scan focus after success; invalid, ambiguous, cancelled, offline, stale, or rejected input produces zero writes;
- keep **`Add more quantity`** separate: it remains the Phase 9 `QUANTITY_ADJUSTED · INCREASE` correction beside quantity already active in the station Area and must never substitute for initial `Receive Quantity`;
- preserve the production build boundary: the real workflow uses server state and never imports or depends on the development-only Scan Station mock.

Not in this corrective phase:

- Worker identity / Worker Sessions / badge-confirmation gates — Phase 13;
- authentication / role enforcement — Phase 14;
- Planned Route template management or PartNumber metadata management — Phase 13;
- Stockroom allocation behavior — already Phase 10.

Implementation state (complete — backend: the `Receive Quantity` command with its read model and API; frontend: the three-view wizard of GUI_DESIGN §4.7 item 1 in the real Scan Station; validated with the full backend gate — `ruff format --check`, `ruff check`, `mypy app tests`, 650 tests —, `alembic upgrade head` + `alembic check` reporting no drift, and the full frontend gate `npm run check` — format, lint, typecheck, 717 tests, build, production-boundary sentinel): **no migration** — the per-type Movement shape check already admits a `RECEIVED` that carries `station_id` and `reason`, so the scan-driven receipt needed no schema change and the Alembic head stays `0011_phase10_stock_allocation`. **Entry condition** (`app/application/intake.py`, surfaced by `app/application/scan_station.py`): a Work Order Demand is ACTIVE while it still has a remaining business shortage — `requested_quantity > allocated_quantity` (PROJECT_PROFILE §14, clarified there in this phase: released quantity never decides it, and `WorkOrder.completed_at` is the aggregate state of the whole Work Order and never the state of one line). The resolution reports `has_active_demand` by that rule plus `intake_available` (no active demand, no ACTIVE QuantityFlow of the PN anywhere, and a station Area that can start production — a terminal Area never can), `part_number_known` (the Step 1 copy tells a known PN from a new one) and `internal_work_orders`; the read model judges the entry condition and the command re-judges it authoritatively when it writes. **The command** (`intake.receive_quantity`, `POST /api/scan-stations/{station_id}/receipts`) is ONE transaction in the established order — input shape → deterministic fingerprint → idempotency fast path → station context → the per-PN advisory lock **shared with the production release** (so a receipt and a release of one PN can never both pass their preconditions) → idempotency re-check → the Scan Station row lock with its authoritative active/binding re-check → the no-active-quantity and no-active-demand preconditions → the internal Work Order resolution under the demand → WorkOrder locks → the locked Area re-read (active, non-terminal) → the Operation lock (`transfers.resolve_arrival_operation` — a single active Operation resolves itself, several take an explicit choice) → the writes → COMMIT (or the replay of a race winner). It creates the PartNumber master on first valid use, the internal `WorkOrder` (`work_order_number = NULL`, `received_date` the site's calendar day), the `WorkOrderDemand` (the optional due date and the operator's reason on a new line), an independent `AssignedRoute` snapshot for `PLANNED` only — its first step must start in the station's Area —, the `QuantityFlow` with `current_area_id` set by the INSERT itself, and the `RECEIVED` Movement carrying the Scan Station identity, the resolved Operation, the receipt reason and the same immutable `context.work_order_demand_id` every release records, so the demand's released quantity and every read model stay derived from one source. **Internal Work Order reuse** (PROJECT_PROFILE §14 "must never guess"): the candidates are the Work Orders without an external number that are not completed and already carry a `MODIFY` demand line for the same PN; exactly one is reused, several are refused with a 409 carrying `selection_required` and the candidates so the station presents the explicit choice, none creates a new internal Work Order, and a `NEW` receipt never reuses. Reuse RAISES that existing line by the received quantity with its `UPDATED` audit row (SLICE1_DATA_MODEL §5 — one canonical PN at most once per Work Order — stays intact; the restricted edit of PROJECT_PROFILE §13 permits raising a released line and editing its due date, and nothing else of an existing line is rewritten). Every refusal writes nothing: an invalid PN, quantity, Request Type, Route Mode or Planned Route, a Route that does not start here, a deactivated or rebound station, a deactivated or terminal Area, a foreign or ambiguous Operation, active demand or active quantity that appeared since the wizard opened, a stale Work Order selection, and a mismatched or cross-command `device_event_id` reuse. **Undo boundary:** a receipt is deliberately NOT reversible from a station (`undo._ineligibility` refuses the `INTAKE` command explicitly, as Phase 10 did for `STOCK`): the Movement-level reversal restores production state and never rewrites the business demand the receipt created or raised, so a half-reversal is impossible by construction. Tests: `tests/test_intake_api.py` (the entry condition with active demand, active quantity and a terminal Area withholding it; the receipt's records, the queued versus direct-processing arrival, the due date, the `PLANNED` snapshot and its refusals; reuse by raising the line with its audit row, the several-candidates refusal with the candidates and the follow-up explicit selection, a stale selection, `NEW` never reusing, a completed Work Order never a candidate; the write-time revalidation and the invalid-input matrix, each with zero writes; replay, mismatched reuse and cross-command reuse; the Undo refusal; the projection replay; and the two concurrent receipts of one PN with a single serial outcome). **Frontend** (`src/api/scan-station.ts` — `receiveQuantity`, `workOrderSelectionRequired`, the widened resolution —, `src/views/scan-station/scan-station-intake-dialog.tsx`, wired in `ScanStationView.tsx`): a PN that resolves with `intake_available` opens `Receive Quantity` instead of the former honest placeholder — the approved three views in ONE dialog lifecycle (settings → quantity → confirmation), the editable defaults `Request Type = MODIFY` and `Route Mode = FLOATING`, the Planned Route field only for `PLANNED` and listing only Routes that start at this Area, the optional due date with the shared `.field-optional` label, the explicit Operation choice when the Area configures several, the reason/notes field, and the Work Order behavior line naming what will be created or reused with the explicit selection inside the settings view when several are plausible (`Next` blocked until one is chosen). The quantity view has no MAX and no default with the shared keypad and recap chips (`TypeChip · RouteModeChip · Operation`), and `Confirm receipt` on the structured summary is the only write point, sent through the shared one-shot write model: success reads only after the server's answer, an explicit refusal keeps the wizard open with the server's reason and nothing recorded, a `selection_required` refusal returns to the settings view with the server's candidates under a NEW `device_event_id` (a refused request wrote nothing, so the corrected intent is a different one — `useOneShotWrite` gained `resetIntent` for exactly that), and a lost response freezes the intent behind the SAME `device_event_id` with `Retry the same receipt`. After the server confirmed, the station context and the Area inventory reload from the server, the barcode input regains focus, and the receipt joins the session log WITHOUT becoming the Undo target. Frontend tests: `scan-station-intake.test.tsx` (the settings defaults and both PN copies, the Area-filtered Planned Routes, the write point with the reload and refocus, the due date / reason / Operation, the `PLANNED` request, the direct-processing arrival, the reuse and the explicit selection, `NEW` never reusing, the refusal, the selection-required refusal with the new id, the lost-response retry under the same id, the offline block, Cancel writing nothing, and the receipt not offered as the Undo target), plus the updated Phase 5 transfer suite where the former "no intake at the station" placeholder is now the wizard. Deliberately absent: Worker identity, Worker Sessions and the badge gates (Phase 13), authorization (Phase 14), Planned Route and PartNumber master management (Phase 13), and any join of received quantity into an existing Quantity Flow — the workflow exists only where the PN has none, and PROJECT_PROFILE §14's explicit join-or-separate confirmation is not collected here, so quantity appearing meanwhile is refused rather than guessed.

## Phase 11 — Read Models and Monitoring Views

- Production Board
- Area Board, including the Manager Summary content in its All Areas overview (no separate Manager Summary view)
- Tracking
- Management → Machines **Assigned now** per-PN quantity breakdown, replacing the Phase 6 total-only temporary presentation
- expected-duration monitoring: replace the Production Board's fixed `>= 3 days` long-dwell stand-in with the canonical advisory expected-duration behavior from PROJECT_PROFILE §17 / GUI_DESIGN. Before implementation, the canonical docs must define which duration source applies when both an Assigned Route step duration and an Operation default are available; the implementation must not invent or silently guess a precedence
- movement-derived projections
- stale-feed and long-data states

Phase 11 remains read-model / monitoring work only. It must not absorb Scan Station production writes, Priority writes, master-data management, Worker sessions, or authentication.

Implementation state (partial — the Production Board is complete end to end; the open Phase 11 work is the Area Board, Tracking, the Machines per-PN assigned breakdown, and expected-duration monitoring): **Backend** (`app/application/production_board.py`, `app/api/production_board.py` — `GET /api/production-board?department_id=`): a read-only Department-wide read model with no per-Area mode (PROJECT_PROFILE §21, GUI_DESIGN §5 — decided post-v18), derived entirely from the current-position projection and the immutable Movement history, never from a stored counter. Active quantity is every ACTIVE QuantityFlow whose current Area belongs to the Department; its holding state follows the flow's effective latest position-bearing Movement and the mode of its Area (`projections.processing_state_of` — the one derivation every Scan Station read model uses; `projections.effective_latest_movements` now resolves the common case in one grouped query and walks the lineage only for a flow created by a lineage event and never moved since), the Machine is that Movement's destination Machine for `ON_MACHINE` quantity and the completing (source) Machine as secondary context for finished quantity, and the entry timestamp (`since`) is that Movement's `occurred_at` — a split child inherits its parent's entry through the lineage, an undone command restores the entry of the state it restored. Quantity is grouped per (Area, state, Machine, External activity) with the OLDEST entry of the group, so a long stay is never hidden by a newer portion; an external Operation (`Operation.is_external`) names the activity. Stocked quantity is the Σ of effective `STOCKED` Movements into the Department's terminal Areas (per Area, without an entry time); scrapped quantity the Σ of effective `SCRAPPED` Movements in the Department's Areas (net of reversed scraps). The demand context is the PN's OPEN Work Order Demands only (the Work Order not completed), in the canonical demand ordering (`allocations.canonical_demand_order`); the first demand defines the row's Hot rank, due date and received date. A completed Work Order is history and never supplies a row's Hot rank, dates or Work Order / Job Number metadata, even while quantity it released is still in production: such a row — like a Phase 9 quantity addition or a merge across demands — has no demand context, keeps a null due date and takes its received date from the day its oldest active flow was created on the site calendar (`SITE_TIMEZONE`). Row selection: a PN with active quantity is always a row; a PN with no active quantity is a row only while it has stocked quantity in the Department AND an open demand, and leaves the board once its every Work Order is complete. Board order (`board_row_sort_key`): exactly the canonical demand ordering of the row's defining demand and nothing else — Hot rank first, dated rows earliest due date first, undated rows after all dated rows by the Work Order's received date, the demand id as the deterministic tie-breaker; stocked quantity is not a sorting tier (an entirely stocked row sorts by its open demand like any other row), and a row without demand context is an unranked, undated row ordered by its fallback received date. The Department: an explicit `department_id` must exist (404); omitted, the single active Department resolves (none 404, several 409 naming them — a display is never silently pointed at the wrong Department). The response carries only fixed source timestamps and dates — dwell times, the due countdown and `Total Days` derive at render from the shared UI clock (GUI_DESIGN §3.12); time in location against the expected duration of the active Route Step is not judged here. Tests: `tests/test_production_board_api.py` (the Department scope and resolution, every state with its Machine and External activity, grouping with the oldest entry, the lineage-inherited and the Undo-restored entry timestamps, stocked and scrapped from history, the footer totals reconciling, the stocked-only row staying while demand is open and leaving on completion, the completed Work Order never supplying the row's context, quantity without demand context keeping its row with the fallback dates, the canonical board order with no stocked tier, and the first demand defining the row's dates). **Frontend** (`src/api/production-board.ts`, `views/production-board/ProductionBoardView.tsx`, `board-feed.ts`, `board-logic.ts`; a real view in `src/app/real-views.ts` shipping in every build, the former mock dataset removed): the board reads `GET /api/production-board` — `?department=<id>` on the board URL (a wall display's configured address, a presentation address rather than a route) becomes `department_id`, otherwise the server resolves the single active Department and its explicit refusal is shown — and refreshes itself every `BOARD_REFRESH_MS` (15 s, `board-logic`) with one request in flight at a time (the next armed after the previous answer); a failed REFRESH keeps the last complete board and marks the feed stale — the title's `● Live` status turns the warning tone with the explicit `Feed stale — reconnecting` note, exactly as while the shared connectivity state is unhealthy — polling continues so the board recovers by itself, connectivity returning after a loss refreshes at once, and a failed FIRST load is the error state with Retry; the board header (Department line from the server's answer, the title with its live status, the clock) renders in every state while the table area shows the loading state, the error state, `No active production in this Department.` or the rows, and the footer (page indicator, rotation, navigation, the server's totals — active PNs, pcs in production, pcs stocked, pcs scrapped —, the Kiosk and Auto scale switches, the sorting legend) renders once a complete board exists. The presentation approved in Phase 2 is unchanged: the location rows (Area dot from the server's Area color, the Machine chip with `on machine`, `queue`, `processing`, the External activity chip, `done` with the completing Machine as tooltip context, `stocked`), the total line with `n scrapped`, the Job Numbers column naming every demand (`<job numbers> · WO <number or —> [· MODIFY] · <n> pcs` or `· allocated a/n`), the derived per-location dwell (`long` at ≥ 3 days), the due countdown and `Total Days` from the shared UI clock, the Hot flame and row tint, kiosk mode, the height-aware pagination with the proportional rotation, the automatic display scaling and every manual navigation; the rows render exactly in the server's order — the former client-side `sortBoardRows` is gone, one ordering rule owned by the read model (the development long-data fixture is ordered by the shared `compareDemandOrder` once at load). Development-only: `?state=loading|empty|error|long` render deterministic states without a request (the long-data fixture is inline behind the DEV boundary — no `src/mocks/` import; the Phase 2 mock dataset `src/mocks/production-board.ts` is gone). Frontend tests: the existing Production Board suite on a fake `GET /api/production-board` answer (the backend wire shape) plus the feed behaviors (the request with and without `department_id`, the Job Numbers column, the loading state under the header, the failed first load with Retry, periodic refresh, the stale feed on a failed refresh with the rows kept and recovery on the next good answer, lost connectivity showing the stale feed and its return refreshing at once, the empty Department, the state previews performing no request) and `production-boundary.test.ts` (the board in the real-view registry, reachable in the production module graph, importing nothing from `src/mocks/`). Deliberately absent from the already-complete Production Board slice: the Area Board and Tracking real views, the Machines per-PN assigned breakdown, and the expected-duration highlight remain Phase 11 work; the PN master name / revision line is Phase 13, per-Department rotation timing and the Due Soon policy are Phase 13, and Hot rank management is Phase 12. Until the expected-duration work is completed, the Production Board's `>= 3 days` `long` flag remains an explicit temporary stand-in, not the final policy.

## Phase 12 — Priority Management

- Hot WorkOrderDemand ranking
- add/search/scan (adding at the bottom applies directly)
- reorder (drag-and-drop, Move Up / Move Down)
- confirmation before removal and before every order change of existing entries — including Undo and Redo (PROJECT_PROFILE §21 Priority Management)
- audited application after explicit confirmation
- Undo/Redo

## Phase 13 — Full Administration and Production Identity Configuration

Completes the Administration view beyond the Phase 3.5 minimum environment setup and makes the remaining production master-data/configuration surfaces real.

- Workers — profiles with stable id, name, badge barcode (the company's existing employee badge, exact-matched; no `PF:WORKER:` barcodes), avatar, and active status; Workers remain separate from Users
- Worker identification per Area — persist and manage the canonical modes `disabled`, `fixed Worker`, and `scanned Worker Session`, including the configured fixed Worker where applicable
- Worker Session runtime integration — badge resolution/switching, the Scan-Station-scoped sliding inactivity session, expiration blocking modal, preservation of open production-dialog drafts, and timeout refresh only on valid production interactions
- production audit identity — add/use the canonical Worker/ScanSession references required by Scan Station Movements and Worker attribution required by station allocations; existing historical records remain valid with null identity and are never backfilled by guessing
- Route Templates — real management through **Management → Planned Routes** (GUI_DESIGN §13), permission-based for authorized production roles; Administration keeps no duplicate Route Templates screen
- Part Numbers — real management of the optional PartNumber master through **Management → Part Numbers** (GUI_DESIGN §14), including metadata editing, image management, the full barcode-label surface, and hard deletion of the metadata record only; deletion never cascades into WorkOrderDemand, QuantityFlow, PartMovement, Allocation, or history, and belongs here rather than Phase 16
- when Part Numbers management becomes real, retarget the Work Orders demand-line PN control to the shared `Edit Part Number` dialog; the barcode label remains reachable from inside it and the control uses the approved edit affordance
- Users and roles / authorization management — configuration is created here; enforcement is Phase 14
- Worker Session policies — one default sliding inactivity timeout with per-Area overrides, plus three independent badge-confirmation options for `DONE`, `QUEUE`, and `UNDO` (default ON) that choose the form of the always-present final confirmation gate
- Undo reason policy — the configuration that activates PROJECT_PROFILE §16 `reason when configured`; when enabled the backend Undo command, not only the UI, requires it
- correction permissions — configured here and enforced in Phase 14
- Department display settings — per-Department Production Board rotation timing (seconds per displayed row and minimum page dwell)
- Due Soon policy settings — replace the temporary frontend default with the configurable policy used by due-date urgency presentation, following GUI_DESIGN §3.12 rather than creating a second policy model
- theme persistence per User and per Scan Station with precedence User → Station → Dark default
- scan-behavior policies, retention/archival policy settings (executed in Phase 16), and general settings

Phase 13 must leave no development-only Worker-session policy preview or fake production identity in the production module graph.

## Phase 14 — Authentication, Role Enforcement, and Authorized Management Corrections

- authenticate application Users; Worker badges remain Scan Station audit identity and never become login credentials
- enforce authorization server-side on every Management, Administration, correction, allocation-adjustment, and master-data write — frontend visibility is never the security boundary
- apply the correction permissions configured in Phase 13 to Undo/correction commands and other privileged production actions
- make the Phase 10 **Management allocation / allocation-reversal** capability a real authorized Management workflow: allocate stocked quantity left for later, inspect allocation history, and append auditable reversals with the required reason; never edit/delete allocation history
- implement the explicitly authorized allocation-correction path permitted by PROJECT_PROFILE §8.12 / §18 when a correction may exceed the ordinary remaining-demand limit; it is a distinct privileged, auditable intent and never a relaxation of routine Stockroom allocation
- implement the missing **AssignedRoute adjustment** workflow for a selected `PLANNED` QuantityFlow (PROJECT_PROFILE §8.10 / §17): authorized user only, explicit reason, previous route state preserved in audit history, selected flow only, Route Template and immutable actual Movement history untouched, and no silent route rewrite after a deviation. Use the canonical route-adjustment audit representation (`ROUTE_ADJUSTED`) when the schema for this phase is introduced
- attach authenticated User identity to Management/Admin audit records and Machine lifecycle actions where canonical actor linkage applies; Scan Station production Movements continue to use Worker identity from Phase 13
- broader Manager/Admin correction capability uses explicit typed/auditable commands; Phase 14 creates no generic edit/delete path for PartMovement history

A route deviation already recorded on the actual `TRANSFERRED` Movement is not duplicated merely to manufacture another deviation event. `ROUTE_ADJUSTED` audits a later authorized AssignedRoute change; actual Movement history remains authoritative.

## Phase 15 — File-Based Work Order Import

- idempotent import
- row validation
- clear partial-failure reporting
- no ERP dependency

## Phase 16 — Deployment, Production Hardening, and Admin Maintenance

Operational requirements and platform paths are defined in [`DEPLOYMENT.md`](./DEPLOYMENT.md). Guidance for running the current repository as restricted internal staging does not make this phase complete.

- backups
- migrations
- HTTPS/internal access
- observability
- rollback
- reconciliation checks
- pilot deployment
- production database-role hardening, including the intended application-role UPDATE/DELETE restrictions on append-only production/audit history
- **administrative Movement-history archival/purge maintenance** (PROJECT_PROFILE §28): configurable retention period, select-by-cutoff → lossless archive export → verify → purge exactly the verified archived rows through a separate privileged maintenance path; keep reversal/reference chains and atomic-command groups whole so no retained row references purged history; include policy / size-threshold / manual triggers, scope preview, mandatory reason, and full audit; normal runtime remains append-only; no full retention/archival engine is built before this phase

PartNumber master hard deletion is **not** a Phase 16 maintenance operation. It belongs to Phase 13 Management → Part Numbers and removes metadata only.

## Deferred

- ERP synchronization
- offline scan synchronization (not part of MVP; production writes stay blocked while disconnected)
- WebSocket/SSE push connectivity (event-driven + polled health detection is the approved mechanism)
- advanced analytics
- speculative automation
- broad ERP/MES features
